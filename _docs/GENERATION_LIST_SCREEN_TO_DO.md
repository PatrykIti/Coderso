### 1. Themes / Theme Profiles

  Design "Themes" admin screen. Sidebar + topbar layout. Header
  with active theme card and toggle between profiles. Grid of
  theme
  profiles with preview thumbnails, tokens summary, and actions
  (activate, duplicate, edit). Right panel shows selected
  profile
  details (colors, typography, spacing). Same admin style and
  icons.

  ### 2. Theme Editor (Profile Details)

  Design "Theme Editor" screen. Top bar with breadcrumbs and
  actions (save, reset, export). Main area split: left live
  preview
  (panel, cards, buttons), right JSON/token editor with
  validation and tabs (colors, typography, spacing, radius).
  Same admin style
  and icons.

  ### 3. Plugin Details

  Design "Plugin Details" screen. Header with plugin name,
  version, status badge, update policy. Tabs for Overview,
  Permissions,
  Changelog, Settings. Overview shows description, author,
  compatibility, install date. Permissions tab with scopes
  list. Same admin
  style and icons.

  ### 4. Plugin Settings (per plugin)

  Design "Plugin Settings" screen. Standard settings layout
  with sections, toggles, input fields, select lists. Include
  save bar
  (sticky) and validation hints. Same admin style and icons.

  ### 5. Media Item Details (Drawer)

  Design "Media Details" drawer screen. Shows preview, metadata
  fields (alt, title, caption), file details (size, mime,
  dimensions),
  usage list. Actions: replace, copy URL, delete. Same admin
  style and icons.

  ### 6. Menu Preview (Public Nav)

  Design "Menu Preview" screen. Split layout: left tree editor,
  right live preview (desktop/mobile toggle). Include
  breadcrumbs, save
  actions, drag/drop hints. Same admin style and icons.

  ### 7. Search Results (Global Search)

  Design "Global Search" screen. Full-page search with results
  grouped by type (pages, entries, media, users). Include
  filters and
  recent searches. Same admin style and icons.

  ### 8. System Status / Health

  Design "System Status" admin screen. Cards for DB, storage,
  email, queue. Show last check, status, quick actions. Table
  of recent
  incidents. Same admin style and icons.

  ### 9. Notifications / Activity

  Design "Notifications" screen. List of recent alerts
  (security, updates, content). Filters for severity and type.
  Right drawer
  shows details. Same admin style and icons.

  ### 10. Profile / Account

  Design "Profile" screen. Personal settings (name, email,
  avatar), password change, sessions list with revoke action.
  Same admin
  style and icons.

  ### 11. Audit Event Details (drawer)

  Design "Audit Event Details" drawer. Show event summary,
  actor, IP, user agent, timestamp. Include JSON payload
  preview and copy
  button. Same admin style and icons.

  ### 12. Content Type Details (Overview)

  Design "Content Type Details" screen. Show schema summary,
  field list, validation rules, usage stats, and quick actions
  (edit
  schema, export). Same admin style and icons.

  ### 13. Roles & Permissions Matrix

  Design "Roles & Permissions" screen. Sidebar + topbar layout.
  Main panel with roles list on left and permission matrix on
  right
  (grid with rows = permissions, columns = roles). Include bulk
  toggles, search, and save bar. Same admin style and icons.

  ### 14. Content Revisions

  Design "Content Revisions" screen. Table with revision
  number, author, timestamp, status. Diff preview button and
  restore action.
  Right drawer shows diff view with added/removed highlights.
  Same admin style and icons.

  ### 15. Page Templates / Layouts

  Design "Page Templates" screen. Grid of templates with
  preview, tags, and actions (create, duplicate, edit). Top
  filters (type,
  status). Same admin style and icons.

  ### 16. Widget Library

  Design "Widget Library" screen. Sidebar with categories
  (hero, grid, forms, media). Main grid of widgets with preview
  cards and
  actions (insert, favorite). Include search and filters. Same
  admin style and icons.

  ### 17. Widget Settings (Editor)

  Design "Widget Settings" screen. Wizard/Visual/Advanced tabs.
  Visual tab shows block preview with contextual form fields.
  Advanced tab shows spacing/padding/visibility. Same admin
  style and icons.

  ### 18. Form Builder

  Design "Form Builder" screen. Left panel with form fields
  library, center canvas for form layout, right sidebar for
  field settings
  (validation, labels, rules). Top actions: save, preview. Same
  admin style and icons.

  ### 19. SEO Manager

  Design "SEO Manager" screen. Table of pages/entries with SEO
  score, title, description, status. Filters and quick edit
  drawer for
  meta title/description and preview snippet. Same admin style
  and icons.

  ### 20. Redirects

  Design "Redirects" screen. Table of redirects (from, to,
  type, status, last hit). Actions: create, edit, disable.
  Drawer with
  redirect form. Same admin style and icons.

  ### 21. Settings - General

  Design "Settings > General" screen. Cards for site name,
  locale, timezone, logo, favicon. Save bar. Same admin style
  and icons.

  ### 22. Settings - Email / SMTP

  Design "Settings > Email" screen. SMTP config fields, test
  email button, status indicator. Same admin style and icons.

  ### 23. Settings - Storage

  Design "Settings > Storage" screen. Radio/cards for Local /
  S3 / Azure. When external selected, show key/secret/bucket
  fields.
  Test connection button. Same admin style and icons.

  ### 24. Settings - Integrations

  Design "Settings > Integrations" screen. Cards for common
  services (GA, Slack, Zapier, Sentry). Connect buttons. Same
  admin style
  and icons.

  ### 25. Users - Invite Flow

  Design "Invite User" drawer/modal. Fields: name, email, role,
  permissions preview. Send invite + cancel. Same admin style
  and icons.

  ### 26. Users - Access Logs

  Design "User Access Logs" screen. Table of logins (user, IP,
  device, timestamp, status). Filters and export. Same admin
  style and
  icons.

  ### 27. Security - IP Allowlist

  Design "Security > IP Allowlist" screen. Table of IP ranges,
  labels, status. Add/remove actions. Drawer for new IP range
  with note.
  Same admin style and icons.

  ### 28. Security - Sessions

  Design "Security > Sessions" screen. Table of active sessions
  with device, location, last active. Revoke buttons. Same
  admin style
  and icons.

  ### 29. Security - Login Alerts

  Design "Security > Login Alerts" screen. Toggle email alerts
  for suspicious login, failed attempts threshold, admin-only
  alerts.
  Same admin style and icons.

  ### 30. Import / Export

  Design "Import & Export" screen. Cards for content types/
  pages/media with export options (json/csv). Import dropzone
  with
  instructions and status list. Same admin style and icons.

  ### 31. Backups

  Design "Backups" screen. List of backups with size, date,
  status. Actions: create backup, restore, download. Schedule
  card for
  daily/weekly. Same admin style and icons.

  ### 32. Analytics Overview

  Design "Analytics" screen. KPI cards (visits, conversions,
  top pages). Charts (line/bar) and date range picker. Same
  admin style
  and icons.