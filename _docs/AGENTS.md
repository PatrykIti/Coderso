# Agent Guidelines Reference

The active agent guidelines live in the repository root `AGENTS.md`.

This file exists because the docs index points to `_docs/AGENTS.md` as the
agent-guideline reference. Keep operational rules in the root file so agents do
not drift between duplicate instruction sources.
