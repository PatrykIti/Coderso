# Audit — Button block (Page Editor V2)

## 1. Meta
- **Target:** Button block (palette: "Clickable call to action")
- **Kind:** block · **Type:** `button`
- **Date:** 2026-06-10 · **Squad/session:** blkB
- **Audit HEAD:** `a06049ba` (live tree was advancing during run — concurrent 418 sanity fixes by another agent; observations reflect that snapshot)
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/button.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/button.png`

## 2. Insert
- **Inserted:** YES. `editorSections=1`, `editorBlocks=1`.
- **Toolbar label:** `Learn more tools` (driven by the block's `label` prop; default label "Learn more").
- **Container:** the button block lands inside an auto-created `content` section (`data-page-block="button"`, `data-page-editor-block-path="root:0"`), already `data-selected="true"` after insert.
- **Default block props (persisted):** `{label:"Learn more", href:"/", target:..., variant:..., size:...}` — confirmed via stored `publishedData.sections[0].blocks[0]`.

## 3. Control inventory (per panel)

### Layout panel — counts: select 2 / number 0 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | native-select[auto\|full] | .seg pills | DRIFT |
| Align | segmented (text-align/block align) | native-select[left\|center\|right] | .seg pills | DRIFT |

### Content panel — counts: select 3 / number 0 / text 2 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Primary text (label) | text | text-input | text input | OK |
| Button URL (href) | text | text-input | text input | OK |
| Target | select (button target) | native-select[self\|blank] | .seg pills | PARTIAL DRIFT |
| Variant | select (button variant) | native-select[primary\|secondary\|ghost\|link] | .seg pills | PARTIAL DRIFT |
| Size | select (button size) | native-select[sm\|md\|lg] | .seg pills | PARTIAL DRIFT |

### Style panel — counts: select 1 / number 2 / text 2 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color (textColor) | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Opacity | number | native-number | text/number | ACCEPTABLE |
| Radius | number radius | native-number | .slider range | DRIFT |
| Shadow | select (shadow) | native-select[none\|sm\|md\|lg] | .seg pills | PARTIAL DRIFT |
| Border color | color (borderColor) | text-input (raw hex) | .swatch swatches + picker | DRIFT |

### Background panel — counts: select 1 / number 0 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background (color) | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Background type | select (bgType) | native-select[none\|color\|gradient\|image\|video] | .seg pills | PARTIAL DRIFT |

### Spacing panel — counts: select 0 / number 8 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding top/right/bottom/left | number padding | native-number | text px (.inp.mono) | ACCEPTABLE |
| Margin top/right/bottom/left | number margin | native-number | text px (.inp.mono) | ACCEPTABLE |

### Responsive panel — counts: all 0 (no fields). Panel renders empty.

### Visibility panel — counts: select 1 / number 0 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch | native-select[yes\|no] | .sw toggle | DRIFT |

**Counts summary (all panels):** `switch=0`, `swatch=0`, `range(slider)=0`, `segmentedGroup=0`. ZERO dedicated controls — every control is one of three native primitives (text / number / select).

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Setting the enclosing section background to `#ff0000` paints red on the canvas. TASK-417 reported this BROKEN → now **FIXED**.
- **Block real render vs placeholder:** REAL render. On the front the button block is a real anchor/button bearing the label text; `placeholderHits=[]`.
- **Persistence:** the inserted button + its `content` section persisted into `publishedData.sections` (verified via page detail API — `secs:1`, button block with full props). Save and Publish both succeeded ("Draft saved." / page `status=published`).

## 5. Public runtime
- **URL:** http://coderso-a.localhost:3000/audit-button-0610-blkb
- **sectionTypes:** `[content]`
- **blockTypes:** `[button]`
- **placeholderHits:** `[]` (none)
- **bodyText:** "Learn more"

## 6. Floating-panel drift vs reference
- **No .seg pill groups** — Width, Align, Target, Variant, Size, Shadow, Background type are all native `<select>`.
- **No .swatch color swatches/picker** — Text color, Border color, Background color are raw-hex text inputs.
- **No .slider range** — Radius is a number input (reference uses a range slider).
- **No .sw toggle** — Visible is a yes/no `<select>`.
- Text props (label, href) and px number props (padding/margin/opacity) are acceptable as rendered.

## 7. Verdict
**PARTIAL** · severity **medium** — the button block inserts, persists, and renders correctly on the published front (real button, not placeholder), and canvas WYSIWYG background works (417 fix confirmed). The single systemic gap is the inspector: it has **zero dedicated controls** (`switch=swatch=slider=segmentedGroup=0`) — Variant/Size/Target/Width/Align collapse to native selects, colors are raw-hex text inputs, radius is a number, and Visible is a yes/no select instead of the reference's .seg pills / .swatch / .slider / .sw toggle. This is exactly the user's "everything is plaintext or numeric to type in" complaint.
