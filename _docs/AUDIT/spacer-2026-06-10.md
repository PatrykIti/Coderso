# Audit — Spacer block (Page Editor V2)

## 1. Meta
- **Target:** Spacer block (palette: "Vertical rhythm control")
- **Kind:** block · **Type:** `spacer`
- **Date:** 2026-06-10 · **Squad/session:** blkB
- **Audit HEAD:** `a06049ba` (live tree was advancing during run — concurrent 418 sanity fixes by another agent; observations reflect that snapshot)
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/spacer.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/spacer.png`

## 2. Insert
- **Inserted:** YES. `editorSections=1`, `editorBlocks=1`.
- **Toolbar label:** `spacer tools`.
- **Container:** auto-created `content` section, `data-page-block="spacer"`, selected after insert.
- **Default block props (persisted):** `{size:32}`.

## 3. Control inventory (per panel)

### Layout panel — counts: select 2 / number 0 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | native-select[auto\|full] | .seg pills | DRIFT |
| Align | segmented | native-select[left\|center\|right] | .seg pills | DRIFT |

### Content panel — counts: select 0 / number 1 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Size | number (clamp 0..240) | native-number | text px (.inp.mono) | ACCEPTABLE |

### Style panel — counts: select 1 / number 2 / text 2 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color (textColor) | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Opacity | number | native-number | text/number | ACCEPTABLE |
| Radius | number radius | native-number | .slider range | DRIFT |
| Shadow | select (shadow) | native-select[none\|sm\|md\|lg] | .seg pills | PARTIAL DRIFT |
| Border color | color (borderColor) | text-input (raw hex) | .swatch swatches + picker | DRIFT |

### Background panel — counts: select 1 / number 0 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background (color) | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Background type | select (bgType) | native-select[none\|color\|gradient\|image\|video] | .seg pills | PARTIAL DRIFT |

### Spacing panel — counts: select 0 / number 8 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding top/right/bottom/left | number padding | native-number | text px (.inp.mono) | ACCEPTABLE |
| Margin top/right/bottom/left | number margin | native-number | text px (.inp.mono) | ACCEPTABLE |

### Responsive panel — counts: all 0 (no fields). Panel renders empty.

### Visibility panel — counts: select 1 / number 0 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch | native-select[yes\|no] | .sw toggle | DRIFT |

**Counts summary (all panels):** `switch=0`, `swatch=0`, `range(slider)=0`, `segmentedGroup=0`. ZERO dedicated controls.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Section background `#ff0000` paints red on the canvas. TASK-417 BROKEN → now **FIXED**.
- **Block real render vs placeholder:** REAL render. The spacer persisted (`publishedData.sections=1`, `blocks=[{type:spacer, props:{size:32}}]`) and renders on the front as a real `<div aria-hidden="true" style="height:32px"></div>`. `placeholderHits=[]`. Like the divider (and unlike the empty list), the spacer is NOT pruned despite having no text.
- **Background persistence (bonus):** section background `#ff0000` from the WYSIWYG probe persisted to the front (block wrapper carries `style="--coderso-block-surface:#ff0000;background-color:#ff0000"`).

## 5. Public runtime
- **URL:** http://coderso-a.localhost:3000/audit-spacer-0610-blkb
- **sectionTypes:** `[content]`
- **blockTypes:** `[spacer]`
- **placeholderHits:** `[]` (none)
- **bodyText:** "" (empty — spacer is non-textual, expected)

## 6. Floating-panel drift vs reference
- **`Size`** as a native number input is ACCEPTABLE (reference uses a px text input). NOTE: the reference's vertical-rhythm size control in the design is a `.slider`; if the intended control were a slider this would be DRIFT, but the registry declares `input: "number"`, so per the drift table it is ACCEPTABLE.
- **No .seg pill groups** — Width, Align, Shadow, Background type are native `<select>`.
- **No .swatch color swatches/picker** — Text color, Border color, Background color are raw-hex text inputs.
- **No .slider range** — Radius is a number input.
- **No .sw toggle** — Visible is a yes/no `<select>`.

## 7. Verdict
**PARTIAL** · severity **low** — the spacer inserts, persists, and renders a correct fixed-height div on the published front (real, not placeholder); canvas WYSIWYG and bg persistence both work (417 fix confirmed). Its one own control (Size number, clamp 0..240) is acceptable. The remaining gap is the shared inspector having **zero dedicated controls** (`switch=swatch=slider=segmentedGroup=0`): colors as raw hex, radius as number, Visible as a yes/no select instead of swatch/slider/.sw toggle. Severity low because spacer's config surface is minimal and its core function works end-to-end.
