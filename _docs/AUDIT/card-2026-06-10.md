# Page Editor V2 — Audit: `card` block

## 1. Meta
- **Target:** `card` block — palette needle "Compact title and body block"
- **Kind:** block
- **Date:** 2026-06-10
- **Audit HEAD:** `a06049ba` (TASK-418 closed). Observations reflect this snapshot; another agent may be concurrently applying 418 sanity fixes.
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/card.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/card.png`
- **Editor URL:** `http://coderso-a.localhost:5173/admin/pages/518d123a-f761-4c57-a169-11aba05df6ce`

## 2. Insert
- Inserted: **YES**. `editorSections=1`, `editorBlocks=1`.
- Toolbar label: **`Card title tools`**.
- Auto-wrapped in a host `content` section (front `sectionTypes:["content"]`), default single `card` block. Front bodyText snippet `"Card title"` confirms the seeded default card.

## 3. Control inventory

### Layout panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | `native-select[auto\|full]` | `.seg` pill buttons | DRIFT |
| Align | segmented (align) | `native-select[left\|center\|right]` | `.seg` pill buttons | DRIFT |

### Content panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Title | text | `text-input` | text input | OK |
| Body | text | `text-input` | text input | OK |
| Image | media (card image) | `text-input` (raw URL) | media picker button | DRIFT |
| Link URL | url | `text-input` | text input | OK |

### Style panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color (textColor) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |
| Opacity | number | `native-number` | text px input | ACCEPTABLE |
| Radius | number radius | `native-number` | `.slider` range | DRIFT |
| Shadow | select (shadow) | `native-select[none\|sm\|md\|lg]` | `.seg` pills | PARTIAL DRIFT |
| Border color | color (borderColor) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |

### Background panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background | color (background) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |
| Background type | select (bgType) | `native-select[none\|color\|gradient\|image\|video]` | `.seg` pills | PARTIAL DRIFT |

### Spacing panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding top/right/bottom/left | number padding | `native-number` (4×) | text px input | ACCEPTABLE |
| Margin top/right/bottom/left | number margin | `native-number` (4×) | text px input | ACCEPTABLE |

### Responsive panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| _(none)_ | — | empty panel | — | EMPTY |

### Visibility panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch (visible) | `native-select[yes\|no]` | `.sw` toggle switch | DRIFT |

**Counts summary (aggregate across panels):** `switch=0`, `swatch=0`, `range=0`, `segmentedGroup=0` in **every** panel. NO dedicated controls exist — segmented/toggle/swatch/slider widgets are all rendered as native `<select>` / `<input type=text>` / `<input type=number>`.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Setting the host section background to `#ff0000` paints red on the canvas. TASK-417 flagged this as BROKEN — it is now **FIXED**.
- **Block render vs placeholder (front):** Real render. `blockTypes:["card"]`, `placeholderHits:[]` — no placeholder/pending/unavailable markers. The card block resolves to a real front component rendering the title ("Card title").

## 5. Public runtime
- **Front URL:** `http://coderso-a.localhost:3000/audit-card-0610-blkc`
- **sectionTypes:** `["content"]`
- **blockTypes:** `["card"]`
- **placeholderHits:** `[]`
- **bodyText snippet:** `"Card title"`

## 6. Floating-panel drift vs reference
Dedicated-control gaps for this block's panels:
- **Image** (media) → raw text URL instead of a media picker button.
- **Width / Align** (segmented) → native `<select>` instead of `.seg` pill buttons.
- **Shadow / Background type** (select) → native `<select>` instead of `.seg` pills.
- **Text color / Border color / Background** (color) → raw hex `text-input` instead of `.swatch` swatches + picker.
- **Radius** (number radius) → `native-number` instead of `.slider` range.
- **Visible** (switch) → yes/no `<select>` instead of `.sw` toggle.

## 7. Verdict
**PARTIAL** — severity **medium**. Card block inserts, edits, WYSIWYG-paints, saves/publishes and renders for real on the public front (no placeholder, shows the title), but every dedicated inspector control from the reference (media picker, pills, color swatches, radius slider, visibility toggle) is collapsed to a native primitive — `switch/swatch/range/segmentedGroup` counts all 0.
