# Page Editor V2 audit — block `group`

## 1. Meta
- **Target:** block `group` (palette needle "Nested grouped layout")
- **Kind:** block
- **Date:** 2026-06-10
- **Audit HEAD:** a06049ba (working tree at 1fb8604a during run — concurrent 418 sanity fixes; observations reflect that snapshot)
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/group.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/group.png`
- **Editor page:** `/admin/pages/306fcc13-22dd-46e5-8c1d-0f2b8273c005`

## 2. Insert
- Inserted: **YES**. `editorSections=1`, `editorBlocks=1`.
- Toolbar label: **`group tools`**.
- Default blocks: a single `group` block in one auto-created `content` section.
- Persisted after reopen: **YES** (`s=1/b=1`, status PUBLISHED) — published front renders the block.

## 3. Control inventory

### Layout panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | native-select[auto\|full] | `.seg` pills | DRIFT |
| Align | segmented | native-select[left\|center\|right] | `.seg` pills | DRIFT |
| Direction | segmented (group direction) | native-select[row\|column] | `.seg` pills | DRIFT |
| Wrap | switch (group wrap) | native-select[yes\|no] | `.sw` toggle | DRIFT |

### Content panel
Empty (no fields).

### Style panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color | text-input (raw hex) | `.swatch` + picker | DRIFT |
| Opacity | number | native-number | text px input | ACCEPTABLE |
| Radius | number radius | native-number | `.slider` range | DRIFT |
| Shadow | select | native-select[none\|sm\|md\|lg] | `.seg` pills | PARTIAL DRIFT |
| Border color | color | text-input (raw hex) | `.swatch` + picker | DRIFT |

### Background panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background | color | text-input (raw hex) | `.swatch` + picker | DRIFT |
| Background type | select (bgType) | native-select[none\|color\|gradient\|image\|video] | `.seg` pills | PARTIAL DRIFT |

### Spacing panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding ×4 / Margin ×4 | number padding/margin | native-number | text px input | ACCEPTABLE |
| Group gap | number gap | native-number | `.slider` range | DRIFT |

### Responsive panel
Empty (no fields).

### Visibility panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch | native-select[yes\|no] | `.sw` toggle | DRIFT |

### Counts summary
Layout `select=4` (Width/Align/Direction/Wrap), Spacing `Group gap` as number, and **`range=0, switch=0, swatch=0, segmentedGroup=0` in EVERY panel** → no dedicated controls; everything collapsed to native primitives. Group is the worst Layout-panel offender: two segmented controls (Direction, Width/Align) and a switch (Wrap) all rendered as native `<select>`.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1` → section background `#ff0000` paints on canvas. TASK-417 reported this BROKEN; now **FIXED**.
- **Block render vs placeholder:** real render. After Save + Publish + reopen, editor shows `s=1/b=1` (PUBLISHED) and front shows `data-page-block="group"` with **no placeholderHits** — rendered by the public runtime, not a placeholder.

## 5. Public runtime
- **Front URL:** `http://coderso-a.localhost:3000/audit-group-0610-blkd`
- **sectionTypes:** `["content"]`
- **blockTypes:** `["group"]`
- **placeholderHits:** `[]`
- **bodyText:** empty (no inner content authored; structural element renders — confirmed via curl `data-page-block="group"`).

## 6. Floating-panel drift vs reference
- Width / Align / Direction → should be `.seg` pill segmented controls, rendered as native `<select>`.
- Wrap → should be a `.sw` toggle switch, rendered as a yes/no `<select>`.
- Group gap → should be a `.slider` range, rendered as a number input.
- Radius → should be a `.slider` range, rendered as a number input.
- Text/Border/Background color → should be `.swatch` + picker, rendered as raw-hex text inputs.
- Background type / Shadow → `.seg` pills rendered as `<select>` (partial).
- Visible → `.sw` toggle rendered as yes/no `<select>`.

## 7. Verdict
**PARTIAL** — severity **medium**. The group block inserts, persists, publishes, and renders on the front, and canvas WYSIWYG works; but its inspector has the heaviest segmented/switch drift (Direction + Wrap + Width + Align), and across all panels zero dedicated controls exist (`swatch=switch=range=segmentedGroup=0`).
