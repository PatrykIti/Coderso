# Audit — Heading block (Page Editor V2)

## 1. Meta
- **Target:** Heading block (palette: "Section title or subheading")
- **Kind:** block · **Type:** `heading`
- **Date:** 2026-06-10 · **Squad/session:** blkA
- **Audit HEAD:** `a06049ba` (live tree was at `1fb8604a` during run — another agent was concurrently applying 418 sanity fixes; observations reflect that snapshot).
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/heading.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/heading.png`
- **Note on method:** the mechanical driver `audit_target.sh` could not create pages during this run — the shared `auth.json` produced an **"Invalid CSRF token"** on the create-page POST, and the `/admin/api/auth/csrf` endpoint was intermittently rate-limited (HTTP 429) by concurrent audit agents. I drove playwright-cli directly via the helpers after warming the CSRF token (poll until 200) and re-authenticating. All evidence below is from the live browser session, session `blkA`.

## 2. Insert
- **Inserted:** YES. `editorSections=1`, `editorBlocks=1`.
- **Toolbar label:** `Heading tools`.
- **Default blocks:** the heading block is inserted into a `content` section wrapper (front shows `sectionTypes=[content]`, `blockTypes=[heading]`). A single heading block, no sub-blocks.

## 3. Control inventory (per panel)

### Layout panel — counts: select 2 / number 0 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | native-select[auto\|full] | .seg pills | DRIFT |
| Align | segmented (text-align) | native-select[left\|center\|right] | .seg pills | DRIFT |

### Content panel — counts: select 2 / number 0 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Primary text | text | text-input | text input | OK |
| Level | select (heading level) | native-select[h1..h6] | .seg pills | PARTIAL DRIFT |
| Text align | segmented | native-select[left\|center\|right] | .seg pills | DRIFT |

### Style panel — counts: select 1 / number 2 / text 2 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Opacity | number | native-number | text px / slider | ACCEPTABLE |
| Radius | number radius | native-number | .slider range | DRIFT |
| Shadow | select | native-select[none\|sm\|md\|lg] | .seg pills | PARTIAL DRIFT |
| Border color | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |

### Background panel — counts: select 1 / number 0 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background (color) | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Background type | select (bgType) | native-select[none\|color\|gradient\|image\|video] | .seg pills | PARTIAL DRIFT |

### Spacing panel — counts: select 0 / number 8 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding top/right/bottom/left | number padding | native-number | text px (.inp.mono) | ACCEPTABLE |
| Margin top/right/bottom/left | number margin | native-number | text px (.inp.mono) | ACCEPTABLE |

### Responsive panel — counts: all 0 (no fields). Panel renders empty.

### Visibility panel — counts: select 1 / number 0 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch | native-select[yes\|no] | .sw toggle | DRIFT |

**Counts summary (all panels):** `switch=0`, `swatch=0`, `range(slider)=0`, `segmentedGroup=0`. ZERO dedicated controls — every control collapses into a native select / number / text primitive. This is the headline drift the user reported ("everything is plaintext or numeric to type in").

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Setting the parent section background to `#ff0000` paints red on the canvas. TASK-417 reported this BROKEN → now **FIXED**.
- **Block render vs placeholder (front):** REAL render, not placeholder. `placeholderHits=[]`; the heading text "Audit Heading Live 0610" renders as live `blockTypes=[heading]` content inside a `content` section.
- **Persistence note:** Save persists correctly after reload (`sections=1, blocks=1, blockText="Audit Heading Live 0610"`). Publish required a second pass — the first Publish ran while the page was still empty and pushed empty `published_data` (front showed "Not Found"); after Save + re-Publish the content appeared. No save toast is shown, but the data persists.

## 5. Public runtime
- **URL:** http://coderso-a.localhost:3000/audit-heading-0610-blka
- **sectionTypes:** `[content]`
- **blockTypes:** `[heading]`
- **placeholderHits:** `[]` (none)
- **bodyText:** "Audit Heading Live 0610"

## 6. Floating-panel drift vs reference
- **No .seg pill groups** — Width, Align, Text align, Level, Shadow, Background type are native `<select>`.
- **No .swatch color swatches/picker** — Text color, Border color, Background color are raw hex text inputs.
- **No .slider range** — Radius is a number input.
- **No .sw toggle** — Visible is a yes/no `<select>`.

## 7. Verdict
**PARTIAL** · severity **medium** — Heading inserts, persists, and renders live on the front (no placeholder), and canvas WYSIWYG background paints (417 fix confirmed). The single gap is control fidelity: the inspector has zero dedicated controls (`switch=swatch=slider=segmentedGroup=0`) — every input is a native select / number / text primitive instead of the reference's seg pills, swatches, sliders and toggles.
