# Audit — Hero section (Page Editor V2)

## 1. Meta
- **Target:** Hero section (palette: "Headline, copy, and primary action")
- **Kind:** section · **Type:** `hero`
- **Date:** 2026-06-10 · **Squad/session:** secA
- **Audit HEAD:** `a06049ba` (live tree was at `1fb8604a` during run — a concurrent agent was applying 418 sanity fixes; observations reflect that snapshot)
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/hero.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/hero.png`

## 2. Insert
- **Inserted:** YES. `editorSections=1`, `editorBlocks=3`.
- **Toolbar label:** `Hero tools`.
- **Default blocks:** heading + text + button (confirmed on front as `blockTypes=[heading,text,button]`).

## 3. Control inventory (per panel)

### Layout panel — counts: select 3 / number 2 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Columns | number | native-number | text px | ACCEPTABLE |
| Max width | number | native-number | text px | ACCEPTABLE |
| Align | segmented | native-select[start\|center\|end\|stretch] | .seg pills | DRIFT |
| Justify | segmented | native-select[start\|center\|end\|between] | .seg pills | DRIFT |
| Variant | select | native-select[default\|split\|centered\|full-width] | .seg pills | PARTIAL DRIFT |

### Content panel — counts: select 2 / number 0 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Primary text | text | text-input | text input | OK |
| Level | select (heading level) | native-select[h1..h6] | .seg pills | PARTIAL DRIFT |
| Text align | segmented | native-select[left\|center\|right] | .seg pills | DRIFT |

### Style panel — counts: select 1 / number 1 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Accent | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Radius | number radius | native-number | .slider range | DRIFT |
| Shadow | select | native-select[none\|sm\|md\|lg] | .seg pills | PARTIAL DRIFT |

### Background panel — counts: select 1 / number 0 / text 2 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background (color) | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Background type | select (bgType) | native-select[none\|color\|gradient\|image\|video] | .seg pills | PARTIAL DRIFT |
| Background (image/media) | media | text-input (raw URL) | media picker button | DRIFT |

### Spacing panel — counts: select 0 / number 5 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Top / Bottom / Left / Right | number padding | native-number | text px (.inp.mono) | ACCEPTABLE |
| Gap | number gap | native-number | .slider range | DRIFT |

### Responsive panel — counts: all 0 (no fields). Panel renders empty.

### Visibility panel — counts: select 2 / number 0 / text 3 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch | native-select[yes\|no] | .sw toggle | DRIFT |
| Auth only | switch | native-select[yes\|no] | .sw toggle | DRIFT |
| Anchor / Starts at / Ends at | text | text-input | text input | OK |

**Counts summary (all panels):** `switch=0`, `swatch=0`, `range(slider)=0`, `segmentedGroup=0`. ZERO dedicated controls exist anywhere — every control collapses to native select / number / text. This is the headline drift.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Setting section background type=color, value `#ff0000` paints red on the canvas. TASK-417 reported this BROKEN; it is now **FIXED** (canvas reflects the section background live).
- **Variant options (hero):** `[default, split, centered, full-width]`. (Variant-switch render comparison performed on content/cta in this batch; hero variant verified present and selectable.)
- **Front render:** real content rendered (not placeholder) — `blockTypes=[heading,text,button]`, `placeholderHits=[]`.

## 5. Public runtime
- **URL:** http://coderso-a.localhost:3000/audit-hero-0610-seca
- **sectionTypes:** `[hero]`
- **blockTypes:** `[heading, text, button]`
- **placeholderHits:** `[]` (none)
- **bodyText:** "Build with Coderso Compose sections and atomic blocks directly on the canvas. Primary action"

## 6. Floating-panel drift vs reference
- **No segmented (.seg) pill groups** — Align, Justify, Text align, Variant, Level, Shadow, Background type all rendered as native `<select>`.
- **No color swatches (.swatch) / picker** — Accent and Background color are raw hex text inputs.
- **No range sliders (.slider)** — Radius and Gap are number inputs (reference uses sliders).
- **No toggle switches (.sw)** — Visible and Auth only are yes/no `<select>`.
- **No media picker** — Background image is a raw URL text input.

## 7. Verdict
**PARTIAL** · severity **medium** — Hero inserts, renders faithfully on the front, and canvas WYSIWYG background now works (417 fix confirmed); but the entire floating inspector is collapsed into native primitives — `switch=swatch=slider=segmentedGroup=0` — so it does not match the dedicated-control reference design.
