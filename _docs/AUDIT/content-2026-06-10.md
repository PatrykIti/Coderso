# Audit — Content section (Page Editor V2)

## 1. Meta
- **Target:** Content section (palette: "Simple text-led section")
- **Kind:** section · **Type:** `content`
- **Date:** 2026-06-10 · **Squad/session:** secA
- **Audit HEAD:** `a06049ba` (live tree was at `1fb8604a` during run — concurrent 418 sanity fixes in progress; observations reflect that snapshot)
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/content.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/content.png`

## 2. Insert
- **Inserted:** YES. `editorSections=1`, `editorBlocks=2`.
- **Toolbar label:** `Content tools`.
- **Default blocks:** heading + text (confirmed on front as `blockTypes=[heading,text]`).

## 3. Control inventory (per panel)

### Layout panel — counts: select 3 / number 2 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Columns | number | native-number | text px | ACCEPTABLE |
| Max width | number | native-number | text px | ACCEPTABLE |
| Align | segmented | native-select[start\|center\|end\|stretch] | .seg pills | DRIFT |
| Justify | segmented | native-select[start\|center\|end\|between] | .seg pills | DRIFT |
| Variant | select | native-select[default\|compact] | .seg pills | PARTIAL DRIFT |

### Content panel — counts: select 2 / number 0 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Primary text | text | text-input | text input | OK |
| Level | select (heading level) | native-select[h1..h6] | .seg pills | PARTIAL DRIFT |
| Text align | segmented | native-select[left\|center\|right] | .seg pills | DRIFT |

### Style panel — counts: select 1 / number 1 / text 1 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Accent | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Radius | number radius | native-number | .slider range | DRIFT |
| Shadow | select | native-select[none\|sm\|md\|lg] | .seg pills | PARTIAL DRIFT |

### Background panel — counts: select 1 / number 0 / text 2 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background (color) | color | text-input (raw hex) | .swatch swatches + picker | DRIFT |
| Background type | select (bgType) | native-select[none\|color\|gradient\|image\|video] | .seg pills | PARTIAL DRIFT |
| Background (image/media) | media | text-input (raw URL) | media picker button | DRIFT |

### Spacing panel — counts: select 0 / number 5 / text 0 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Top / Bottom / Left / Right | number padding | native-number | text px (.inp.mono) | ACCEPTABLE |
| Gap | number gap | native-number | .slider range | DRIFT |

### Responsive panel — counts: all 0 (no fields). Panel renders empty.

### Visibility panel — counts: select 2 / number 0 / text 3 / range 0 / switch 0 / swatch 0 / segmentedGroup 0
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch | native-select[yes\|no] | .sw toggle | DRIFT |
| Auth only | switch | native-select[yes\|no] | .sw toggle | DRIFT |
| Anchor / Starts at / Ends at | text | text-input | text input | OK |

**Counts summary (all panels):** `switch=0`, `swatch=0`, `range(slider)=0`, `segmentedGroup=0`. ZERO dedicated controls — every control is a native select / number / text primitive.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Section background `#ff0000` paints red on the canvas. TASK-417 reported BROKEN → now **FIXED**.
- **Variant options (content):** `[default, compact]`.
- **Variant-switch render check (extra):** switched Variant `default → compact`, saved + published, re-fetched front:
  - `default`  → `data-page-variant=default`, classes `w-full px-4 py-6`
  - `compact`  → `data-page-variant=compact`, classes `w-full px-4 py-6`
  - **Result:** the `data-page-variant` attribute updates and persists, BUT the rendered section template (`data-page-section-template=content`) and CSS classes are **identical**. Switching the Variant does **NOT** change the published front layout — it is a no-op visually. Variant is functional at the data layer only.
- **Front render:** real content (not placeholder). `placeholderHits=[]`.

## 5. Public runtime
- **URL:** http://coderso-a.localhost:3000/audit-content-0610-seca
- **sectionTypes:** `[content]`
- **blockTypes:** `[heading, text]`
- **placeholderHits:** `[]` (none)
- **bodyText:** "content section Add focused content blocks here."

## 6. Floating-panel drift vs reference
- **No .seg pill groups** — Align, Justify, Text align, Variant, Level, Shadow, Background type are native `<select>`.
- **No .swatch color swatches/picker** — Accent and Background color are raw hex text inputs.
- **No .slider range** — Radius and Gap are number inputs.
- **No .sw toggle switches** — Visible and Auth only are yes/no `<select>`.
- **No media picker** — Background image is a raw URL text input.

## 7. Verdict
**PARTIAL** · severity **medium** — Content inserts and renders correctly on the front, and canvas WYSIWYG background works (417 fix confirmed). Two gaps: (1) the inspector has zero dedicated controls (`switch=swatch=slider=segmentedGroup=0`); (2) the Variant select persists `data-page-variant` but produces no layout change on the published front (default vs compact render identical classes).
