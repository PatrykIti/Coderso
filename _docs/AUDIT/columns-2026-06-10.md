# Page Editor V2 audit — block `columns`

## 1. Meta
- **Target:** block `columns` (palette needle "Nested column layout")
- **Kind:** block
- **Date:** 2026-06-10
- **Audit HEAD:** a06049ba (working tree at 1fb8604a during run — concurrent 418 sanity fixes; observations reflect that snapshot)
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/columns.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/columns.png`
- **Editor pages used:** `/admin/pages/711205bf-7a6c-40c9-b114-ad1773a36bb3` (fresh v2) and `/admin/pages/21f9dba9-f3c1-42d2-b519-bf26ff4d8d08` (v1)

## 2. Insert
- Inserted in-session: **YES**. `editorSections=1`, `editorBlocks=1`.
- Toolbar label: **`columns tools`**.
- Default blocks: a single `columns` block in one auto-created `content` section.
- **Persisted after reopen: NO** — `editorSections=0`, `editorBlocks=0`. See Functional checks; this is the headline bug.

## 3. Control inventory

### Layout panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | native-select[auto\|full] | `.seg` pills | DRIFT |
| Align | segmented | native-select[left\|center\|right] | `.seg` pills | DRIFT |
| Column count | number count | native-number | text px input / `.seg` | ACCEPTABLE |
| Distribution | select (columns distribution) | native-select[equal\|auto] | `.seg` pills | PARTIAL DRIFT |

### Content panel
Empty (no fields).

### Style panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color | text-input (raw hex) | `.swatch` + picker | DRIFT |
| Opacity | number | native-number | text px input | ACCEPTABLE |
| Radius | number radius | native-number | `.slider` range | DRIFT |
| Shadow | select | native-select[none\|sm\|md\|lg] | `.seg` pills | PARTIAL DRIFT |
| Border color | color | text-input (raw hex) | `.swatch` + picker | DRIFT |

### Background panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background | color | text-input (raw hex) | `.swatch` + picker | DRIFT |
| Background type | select (bgType) | native-select[none\|color\|gradient\|image\|video] | `.seg` pills | PARTIAL DRIFT |

### Spacing panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding ×4 / Margin ×4 | number padding/margin | native-number | text px input | ACCEPTABLE |
| Column gap | number gap | native-number | `.slider` range | DRIFT |

### Responsive panel
Empty (no fields).

### Visibility panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch | native-select[yes\|no] | `.sw` toggle | DRIFT |

### Counts summary
`select=3 number=1` (Layout), `gap` as number in Spacing, and **`range=0, switch=0, swatch=0, segmentedGroup=0` in EVERY panel** → zero dedicated controls; all collapsed to native primitives.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1` → section background `#ff0000` paints on canvas. TASK-417 reported this BROKEN; now **FIXED**.
- **Block render vs placeholder — BROKEN (does not persist):** Insert reliably produces a live `columns` block in-session (`s=1/b=1`, toolbar `columns tools`), but after Save + reopen the editor shows **`s=0/b=0`** and the published front renders **"This page has no content yet."** with empty `sectionTypes`/`blockTypes`.
  - Reproduced twice on clean inserts: fresh page `711205bf` and original `21f9dba9` — both reopen to `s=0/b=0`.
  - **Control comparison (same session):** `container` and `group` blocks BOTH persisted (`s=1/b=1` after reopen) and rendered on the front. So this is a **columns-specific persistence/serialization failure**, not a generic save bug.

## 5. Public runtime
- **Front URL:** `http://coderso-a.localhost:3000/audit-columns-0610-blkd` (page is PUBLISHED — title resolves)
- **sectionTypes:** `[]`
- **blockTypes:** `[]`
- **placeholderHits:** `[]`
- **bodyText:** `"This page has no content yet."` (independently confirmed via curl — front contains no `data-page-section`/`data-page-block`).

## 6. Floating-panel drift vs reference
- Width / Align / Distribution → should be `.seg` pills, rendered as native `<select>`.
- Column gap → should be a `.slider` range, rendered as a number input.
- Radius → should be a `.slider` range, rendered as a number input.
- Text/Border/Background color → should be `.swatch` + picker, rendered as raw-hex text inputs.
- Background type / Shadow → `.seg` pills rendered as `<select>` (partial).
- Visible → `.sw` toggle rendered as yes/no `<select>`.

## 7. Verdict
**BROKEN** — severity **high**. The `columns` block does not survive a save: it inserts and shows tools in-session but reopening the editor and the published front both show zero content, while sibling layout blocks (container, group) persist fine. On top of that, the inspector exhibits the same total dedicated-control drift (`swatch=switch=range=segmentedGroup=0`).
