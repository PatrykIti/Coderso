# Page Editor V2 — Audit: `image` block

## 1. Meta
- **Target:** `image` block — palette needle "Image from media or URL"
- **Kind:** block
- **Date:** 2026-06-10
- **Audit HEAD:** `a06049ba` (TASK-418 closed). Observations reflect this snapshot; another agent may be concurrently applying 418 sanity fixes.
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/image.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/image.png`
- **Editor URL:** `http://coderso-a.localhost:5173/admin/pages/1b6e6db6-79f0-412c-808a-25cbe240b17a`

## 2. Insert
- Inserted: **YES**. `editorSections=1`, `editorBlocks=1`.
- Toolbar label: **`image tools`**.
- The image block was auto-wrapped in a host `content` section (front `sectionTypes:["content"]`), default single `image` block.

## 3. Control inventory

### Layout panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | `native-select[auto\|full]` | `.seg` pill buttons | DRIFT |
| Align | segmented (align) | `native-select[left\|center\|right]` | `.seg` pill buttons | DRIFT |

### Content panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Source | media (image src) | `text-input` (raw URL) | media picker button | DRIFT |
| Alt text | text | `text-input` | text input | OK |
| Caption | text | `text-input` | text input | OK |
| Fit | select (image fit) | `native-select[cover\|contain]` | `.seg` pills | PARTIAL DRIFT |

### Style panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color (textColor) | `text-input` (raw hex) | `.swatch` color swatches + picker | DRIFT |
| Opacity | number | `native-number` | text px input | ACCEPTABLE |
| Radius | number radius | `native-number` | `.slider` range | DRIFT |
| Shadow | select (shadow) | `native-select[none\|sm\|md\|lg]` | `.seg` pills | PARTIAL DRIFT |
| Border color | color (borderColor) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |

### Background panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background | color (background) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |
| Background type | select (bgType) | `native-select[none\|color\|gradient\|image\|video]` | `.seg` pills | PARTIAL DRIFT |

### Spacing panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding top/right/bottom/left | number padding | `native-number` (4×) | text px input | ACCEPTABLE |
| Margin top/right/bottom/left | number margin | `native-number` (4×) | text px input | ACCEPTABLE |

### Responsive panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| _(none)_ | — | empty panel | — | EMPTY |

### Visibility panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch (visible) | `native-select[yes\|no]` | `.sw` toggle switch | DRIFT |

**Counts summary (aggregate across panels):** `switch=0`, `swatch=0`, `range=0`, `segmentedGroup=0` in **every** panel. NO dedicated controls exist — segmented/toggle/swatch/slider widgets are all rendered as native `<select>` / `<input type=text>` / `<input type=number>`. This is the headline drift.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Setting the host section background to `#ff0000` paints red on the canvas. The TASK-417 report flagged this as BROKEN — it is now **FIXED**.
- **Block render vs placeholder (front):** Real render. `blockTypes:["image"]`, `placeholderHits:[]` — no placeholder/pending/unavailable markers. The image block resolves to a real front component (renders an image element / "Image" caption text with the seeded default source).

## 5. Public runtime
- **Front URL:** `http://coderso-a.localhost:3000/audit-image-0610-blkc`
- **sectionTypes:** `["content"]`
- **blockTypes:** `["image"]`
- **placeholderHits:** `[]`
- **bodyText snippet:** `"Image"`

## 6. Floating-panel drift vs reference
Dedicated-control gaps for this block's panels:
- **Source** (media) → raw text URL instead of a media picker button.
- **Width / Align** (segmented) → native `<select>` instead of `.seg` pill buttons.
- **Fit / Shadow / Background type** (select) → native `<select>` instead of `.seg` pills (functional, but not the designed pill control).
- **Text color / Border color / Background** (color) → raw hex `text-input` instead of `.swatch` swatches + picker.
- **Radius** (number radius) → `native-number` instead of `.slider` range.
- **Visible** (switch) → yes/no `<select>` instead of `.sw` toggle.

## 7. Verdict
**PARTIAL** — severity **medium**. The image block inserts, edits, WYSIWYG-paints, saves/publishes and renders for real on the public front (no placeholder), but every dedicated inspector control from the reference (media picker, pills, color swatches, radius slider, visibility toggle) is collapsed to a native primitive — `switch/swatch/range/segmentedGroup` counts are all 0.
