# Page Editor V2 — Audit: `video` block

## 1. Meta
- **Target:** `video` block — palette needle "Embedded video from media or URL"
- **Kind:** block
- **Date:** 2026-06-10
- **Audit HEAD:** `a06049ba` (TASK-418 closed). Observations reflect this snapshot; another agent may be concurrently applying 418 sanity fixes.
- **Evidence:** `/home/coder/project/Coderso/.tmp/audit/evidence/video.json`
- **Screenshot:** `/home/coder/project/Coderso/.tmp/audit/shots/video.png`
- **Editor URL:** `http://coderso-a.localhost:5173/admin/pages/4838620d-ba9d-427d-9a0d-bd52f529b9fa`

> Note: the deterministic driver bounced to `/admin/login` then `/admin/pages` twice (transient session race from a concurrent agent). This target was therefore driven manually via the `lib.sh` helpers (auth · new_page · palette_insert · classify_panel · WYSIWYG probe · save · publish · front_markers); evidence below is hand-assembled from those live runs.

## 2. Insert
- Inserted: **YES**. `editorSections=1`, `editorBlocks=1`.
- Toolbar label: **`video tools`**.
- Auto-wrapped in a host `content` section (front `sectionTypes:["content"]`), default single `video` block.

## 3. Control inventory

### Layout panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Width | segmented (block width) | `native-select[auto\|full]` | `.seg` pill buttons | DRIFT |
| Align | segmented (align) | `native-select[left\|center\|right]` | `.seg` pill buttons | DRIFT |

### Content panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Source | media (video src) | `text-input` (raw URL) | media picker button | DRIFT |
| Title | text | `text-input` | text input | OK |
| Autoplay | switch (video autoplay) | `native-select[yes\|no]` | `.sw` toggle switch | DRIFT |
| Muted | switch (video muted) | `native-select[yes\|no]` | `.sw` toggle switch | DRIFT |

### Style panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Text color | color (textColor) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |
| Opacity | number | `native-number` | text px input | ACCEPTABLE |
| Radius | number radius | `native-number` | `.slider` range | DRIFT |
| Shadow | select (shadow) | `native-select[none\|sm\|md\|lg]` | `.seg` pills | PARTIAL DRIFT |
| Border color | color (borderColor) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |

### Background panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Background | color (background) | `text-input` (raw hex) | `.swatch` swatches + picker | DRIFT |
| Background type | select (bgType) | `native-select[none\|color\|gradient\|image\|video]` | `.seg` pills | PARTIAL DRIFT |

### Spacing panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Padding top/right/bottom/left | number padding | `native-number` (4×) | text px input | ACCEPTABLE |
| Margin top/right/bottom/left | number margin | `native-number` (4×) | text px input | ACCEPTABLE |

### Responsive panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| _(none)_ | — | empty panel | — | EMPTY |

### Visibility panel
| Control | registry input | rendered widget | reference control | verdict |
|---|---|---|---|---|
| Visible | switch (visible) | `native-select[yes\|no]` | `.sw` toggle switch | DRIFT |

**Counts summary (aggregate across panels):** `switch=0`, `swatch=0`, `range=0`, `segmentedGroup=0` in **every** panel. NO dedicated controls exist. Note the video block has **3** switch-kind registry props (Autoplay, Muted, Visible) — all rendered as yes/no `<select>`, none as `.sw` toggles.

## 4. Functional checks
- **Canvas WYSIWYG:** `canvasRedAfterBgSet=1`. Setting the host section background to `#ff0000` paints red on the canvas. TASK-417 flagged this as BROKEN — it is now **FIXED**.
- **Block render vs placeholder (front):** Real render. `blockTypes:["video"]`, `placeholderHits:[]` — no placeholder/pending/unavailable markers. The video block resolves to a real front component ("Video" rendered).

## 5. Public runtime
- **Front URL:** `http://coderso-a.localhost:3000/audit-video-0610-blkc`
- **sectionTypes:** `["content"]`
- **blockTypes:** `["video"]`
- **placeholderHits:** `[]`
- **bodyText snippet:** `"Video"`

## 6. Floating-panel drift vs reference
Dedicated-control gaps for this block's panels:
- **Source** (media) → raw text URL instead of a media picker button.
- **Autoplay / Muted / Visible** (switch) → yes/no `<select>` instead of `.sw` toggles (3 toggles missing).
- **Width / Align** (segmented) → native `<select>` instead of `.seg` pill buttons.
- **Shadow / Background type** (select) → native `<select>` instead of `.seg` pills.
- **Text color / Border color / Background** (color) → raw hex `text-input` instead of `.swatch` swatches + picker.
- **Radius** (number radius) → `native-number` instead of `.slider` range.

## 7. Verdict
**PARTIAL** — severity **medium**. Video block inserts, edits, WYSIWYG-paints, saves/publishes and renders for real on the front (no placeholder), but every dedicated inspector control is collapsed to a native primitive; notably all three video toggle props (autoplay/muted/visible) are yes/no selects rather than `.sw` switches. `switch/swatch/range/segmentedGroup` counts all 0.
