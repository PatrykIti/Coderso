# 692. TASK-188-01 policy schema

Date: 2026-04-18
Version: unreleased
Tasks: TASK-188-01

## Key Changes

### Assistant/Core

- Added the initial `operationPolicy` foundation with typed policy contracts, strict runtime normalization, and lookup helpers.
- The new policy layer is additive only; planner/resolver runtime behavior is unchanged in this leaf.

## Validation

- `bun run vitest run --config vitest.config.ts tests/vitest/assistant/operation-policy-schema.test.ts tests/vitest/assistant/operation-policy-lookup.test.ts`
- `bun --cwd core lint`
- `bun --cwd core lint:types`
