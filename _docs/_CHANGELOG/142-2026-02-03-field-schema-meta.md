# Field schema meta

- Added `xFieldConfig` to content schema properties and preserved relation/select metadata.
- Updated schema mapping to read/write UI meta for stable field type round-trips.
- Registered `xFieldConfig` as an AJV keyword for strict schema validation.
- Updated content type docs for schema meta fields.
