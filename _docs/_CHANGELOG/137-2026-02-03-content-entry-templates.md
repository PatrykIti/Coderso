# Content entry templates

- Added core content list/detail templates with theme override support.
- Public runtime now resolves content templates via theme resolver with fallback to core defaults.
- Updated public entry rendering to pass content type metadata into templates.
