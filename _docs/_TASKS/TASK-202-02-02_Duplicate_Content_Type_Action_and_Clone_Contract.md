# TASK-202-02-02: Duplicate Content Type Action and Clone Contract
# FileName: TASK-202-02-02_Duplicate_Content_Type_Action_and_Clone_Contract.md

**Priority:** Medium
**Category:** CMS/Engine + Admin/UI + API
**Estimated Effort:** Medium
**Dependencies:** TASK-202-02, TASK-202-01-02
**Status:** To Do

---

## Overview

Add the missing `Duplicate content type` workflow from `UX-6`. The clone copies
the schema structure only and creates a new content type with unique name/slug.

## Sub-Tasks

No child task files.

## Files to Change

- `core/admin/ui/content-types/ContentTypeTable.tsx:104-108`
- `core/admin/ui/content-types/ContentTypeEditor.tsx:318-382`
- `core/admin/services/contentTypesClient.ts:145-198`
- `core/services/content/typeService.ts:63-99` if duplication is server-owned.
- `core/server/routes/contentTypeRoutes.ts:54-82` if adding a duplicate route.
- `tests/vitest/admin/contentTypesClient.test.ts`
- `tests/vitest/ui/content-type-table.test.tsx`
- `tests/integration/routes/contentTypes.test.ts` if route behavior changes.

## Security Contract

- Visibility: internal admin action only.
- Auth model: unchanged admin session/API key.
- RBAC: `content:write`.
- CSRF: required for duplicate mutation.
- Rate-limit bucket: `admin_write`.
- Reject-unknown validation:
  - source id must be explicit,
  - target name/slug must be explicit or generated by the server and returned,
  - unknown fields are rejected.
- Anti-abuse:
  - entries, media, custom screens, listings, and taxonomies are not cloned,
  - clone keeps schema metadata but does not copy runtime records,
  - duplicate target must not overwrite an existing slug.

## Testing Requirements

- Duplicate action creates a schema-only copy.
- Target name/slug are unique and deterministic.
- Cache updates include list and new detail keys.
- Error path handles duplicate target slug cleanly.

## Documentation Updates Required

- `_docs/CONTENT_TYPES_SPEC.md`
- `_docs/CMS_API.md` if route changes.
- `_docs/PLAYWRIGHT/SUMMARY-ENGINE.md` closure mapping.
- `_docs/_TASKS/README.md`

## Acceptance Criteria

1. Admins can duplicate a content type from list or editor surfaces.
2. The duplicate has the same schema and no entries.
3. Cache and navigation point to the new content type.
