# TASK-001: ORM Foundation and Auth Tables
# FileName: TASK-001_ORM_Foundation_and_Auth_Tables.md

**Priority:** High
**Category:** Core/DB
**Estimated Effort:** Medium
**Dependencies:** None
**Status:** To Do

---

## Overview

Set up Drizzle ORM with PostgreSQL and implement base auth tables (users, roles,
sessions). This task establishes the DB foundation used by all other CMS
features.

**Goals:**
- Working DB client and migration pipeline.
- Core auth tables aligned with `DATA_MODEL.md`.
- Seeded initial admin role + user (optional).

---

## Architecture

```
core/db/
  client.ts        # DB connection + drizzle client
  schema.ts        # tables (users, roles, sessions)
  migrations/      # generated by drizzle-kit
```

---

## Sub-Tasks

### TASK-001-1: Initialize Drizzle client and config

**Status:** To Do

Example (client):

```ts
import postgres from "postgres";
import { drizzle } from "drizzle-orm/postgres-js";

const sql = postgres(process.env.DATABASE_URL!, { max: 10 });
export const db = drizzle(sql);
```

**Implementation Checklist:**

| Step | Details |
|------|---------|
| Config | Add `drizzle.config.ts` with schema and migrations path |
| Client | Create `core/db/client.ts` with postgres.js + drizzle |
| Env | Require `DATABASE_URL` |
| Migrations | Add scripts for generate/migrate |

---

### TASK-001-2: Define auth tables

**Status:** To Do

Example (schema excerpt):

```ts
import { pgTable, text, uuid, timestamp, jsonb } from "drizzle-orm/pg-core";

export const users = pgTable("users", {
  id: uuid("id").defaultRandom().primaryKey(),
  email: text("email").notNull().unique(),
  passwordHash: text("password_hash").notNull(),
  name: text("name"),
  status: text("status").notNull().default("active"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
  lastLoginAt: timestamp("last_login_at"),
});
```

Tables to include:
- users
- roles
- user_roles
- sessions

---

### TASK-001-3: Seed initial admin role (optional)

**Status:** To Do

Seed script example:

```ts
await db.insert(roles).values({
  name: "admin",
  permissions: ["*"],
});
```

---

## Testing Requirements

- [ ] DB connection test (can connect with `DATABASE_URL`).
- [ ] Migration apply test (schema exists).
- [ ] Smoke test: insert user + session.

---

## Documentation Updates Required

- `_docs/ORM_SPEC.md` (paths or scripts if changed).
- `_docs/DATA_MODEL.md` (if schema changes vs doc).

---

## Changelog Entry (planned)

- `_docs/_CHANGELOG/{N}-{YYYY-MM-DD}-orm-foundation-and-auth-tables.md`
- Notes: DB client setup, auth tables, migrations.

---

## Additional Docs

- `_docs/AUTH_SPEC.md` (if auth schema differs).
