# TASK-555-07-L01: Aggregate Contract Security and Regression Gates
# FileName: TASK-555-07-L01-Aggregate-Contract-Security-And-Regression-Gates.md

**Parent Subtask:** TASK-555-07
**Priority:** High
**Category:** QA / Security / Reliability / Documentation
**Estimated Effort:** Very Large
**Status:** ⏳ To Do
**Dependencies:** landed TASK-555-06-L03 receipt; terminal TASK-548
source/generator handoff; serialized terminal shared-document writer handoff

---

## Overview

Add aggregate acceptance, security, and reliability proof without reopening
production source. Finish every product/security/cache/developer/Guide source and
generated-documentation byte, including the exact terminal TASK-548 successor
transaction, before the runtime-smoke leaf starts.

## Sub-Tasks

None; this is an executable leaf.

## Scope and Exact Single-Writer Files

Add aggregate acceptance without reopening product source. Sole test/gate writer:
`tests/integration/kits/curatedStarterAcceptance.test.ts`,
`tests/security/curatedStarter.security.test.ts`,
`tests/reliability/curatedStarter.reliability.test.ts`, the TASK-555
reliability/security command entries in `scripts/coderso-release-gates.ts`, and
their bounded assertions in `tests/unit/release/codersoReleaseGates.test.ts`.

This leaf is also the sole final TASK-555 pre-smoke writer for:

- `_docs/SOLUTION_KITS.md`, `_docs/CMS_API.md`, `_docs/CMS_SPEC.md`,
  `_docs/ARCHITECTURE.md`, `_docs/SECURITY_SPEC.md`, `_docs/DATA_MODEL.md`,
  `_docs/RELEASE_PROCESS.md`, `_docs/CODERSO_RELEASE_GATES.md`,
  `_docs/ADMIN_CACHE.md`, and `_docs/ADMIN_CACHE_MAP.md`;
- `docs/develop/full-site-packages.md`,
  `docs/develop/runtime-smoke-cookbook.md`, and `docs/develop/README.md`;
- `README.md`, `docs/README.md`, and `docs/guide/README.md` where their shipped
  starter/provider/index truth changes;
- `docs/guide/coderso/solution-kits.md`,
  `docs/guide/playbooks/solution-kit-selection-guide.md`,
  `docs/guide/getting-started/admin-orientation.md`, and
  `docs/guide/getting-started/site-setup-and-first-publish.md`;
- exact additive TASK-555 records in terminal
  `docs/guide/capabilities/atomic-controls.v1.json`,
  `docs/guide/capabilities/composed-workflows.v1.json`, and
  `docs/guide/capabilities/section-bindings.v1.json`;
- every tool-owned generated documentation byte named by terminal TASK-548's
  successor receipt, including `core/generated/docs/coderso-docs-v2.json`,
  `core/generated/docs/coderso-docs-coverage-v2.json`, and
  `docs/guide/_COVERAGE_MATRIX.md`.

All repaired TASK-555 handoffs name this leaf as the pre-smoke documentation writer.
TASK-555-07-L03 is closure metadata only and may not be reinterpreted as a product,
developer, Guide, or generated-documentation writer. Any newly discovered contrary
handoff is contract drift and blocks implementation until its owning task contract is
repaired.

## Forbidden Paths

All product production source, task families other than this file, task/changelog
indexes and closure metadata, shared smoke/workflow/evidence, root config, TMP, and
foreign tests. Do not edit TASK-414/489/545/547/548/551/554/556 files or any foreign
changelog. Do not edit or regenerate
`core/generated/cms/coderso-cms-capabilities-v1.json` and do not add another Guide,
capability, compiler, or generator contract.

## Security Contract

Exercise internal session routes and existing public Form only. Assert exact RBAC,
CSRF, admin_read/admin_write, strict reject-unknown, cross-actor preview non-disclosure,
idempotency redaction, lineage reservation fencing, and no public starter write. The separate legacy
catalog/planner contract remains byte-behavior compatible under its terminal
shared transport policy. Public Form retains nonce/CAPTCHA policy.

Documentation must state those same boundaries without exposing claims, package
bodies, snapshots, actor identities, form payloads, provider credentials, or raw
evidence. Generated outputs are tool-owned and must be produced only by the terminal
TASK-548 transaction.

## Terminal TASK-548 Successor Transaction

Before the first Guide or generated-doc write, read terminal TASK-548's handoff and
freeze its exact landed source/output inventory, recovery rules, and ordered write/check
argv in the L01 start receipt. A missing, renamed, or changed seam blocks this leaf;
do not guess an alias, edit generated bytes by hand, or create a second compiler.

1. Finish every human-authored documentation byte listed above, including exact
   additive records in `atomic-controls.v1.json`, `composed-workflows.v1.json`, and
   `section-bindings.v1.json`. Preserve every pre-TASK-555 record byte-for-byte. Add
   exactly these twelve atomic control IDs:
   - `docs.control.solution-kits.curated-starter-select`;
   - `docs.control.solution-kits.curated-starter-preview`;
   - `docs.control.solution-kits.curated-starter-apply`;
   - `docs.control.solution-kits.curated-starter-validate`;
   - `docs.control.solution-kits.curated-starter-open-site`;
   - `docs.control.solution-kits.curated-starter-rollback`;
   - `docs.control.setup.curated-starter-select`;
   - `docs.control.setup.curated-starter-preview`;
   - `docs.control.setup.curated-starter-apply`;
   - `docs.control.setup.curated-starter-validate`;
   - `docs.control.setup.finish`;
   - `docs.control.setup.curated-starter-rollback`.
   Add exactly workflows
   `docs.workflow.solution-kits.curated-starter-reviewed-install` and
   `docs.workflow.setup.curated-starter-reviewed-install`. Their happy-path atom
   orders are respectively `select -> preview -> apply -> validate -> open-site` and
   `select -> preview -> apply -> validate -> finish`; both rollback atoms are
   section-bound conditional recovery controls and are not unconditional workflow
   steps.
2. Pin every Solution Kits atom/workflow relation to exact source
   `docs/guide/coderso/solution-kits.md` and section
   `{docId:"coderso-solution-kits",locale:"en",sectionId:"reviewed-curated-starter-install"}`.
   Pin every Setup atom/workflow relation to exact source
   `docs/guide/getting-started/site-setup-and-first-publish.md` and section
   `{docId:"getting-started-site-setup-and-first-publish",locale:"en",sectionId:"reviewed-curated-starter-setup"}`.
   No filename/title/heading inference is allowed.
3. Without an intervening source mutation, run terminal TASK-548's exact unchanged
   recovery/write sequence for its permission snapshot, composition catalog, packaged
   bundle/source hash, and coverage/link/route/publication outputs.
4. Immediately run every corresponding terminal TASK-548 read-only check in the exact
   recorded order. Validate
   `core/services/kits/curatedStarters/contributions/curatedStarterCapabilitySourceAdapter.ts`
   and parity-check its five sibling descriptor owners:
   `core/server/routes/contributions/curatedStarterRouteContribution.ts`,
   `core/services/kits/curatedStarters/contributions/curatedStarterAdminControlContribution.ts`,
   `core/services/kits/curatedStarters/contributions/curatedStarterNativeContribution.ts`,
   `core/services/kits/curatedStarters/contributions/curatedStarterCacheContribution.ts`,
   and
   `core/services/kits/curatedStarters/contributions/curatedStarterSmokeContribution.ts`.
   Require their exact descriptor IDs
   `core:solution-kits/curated-starter-routes`,
   `core:solution-kits/curated-starter-controls`,
   `core:solution-kits/curated-starter-domain`,
   `core:solution-kits/curated-starter-admin-cache`,
   `core:solution-kits/curated-starter-runtime-smoke`, and
   `core:solution-kits/curated-starter-lifecycle`. Remaining TASK-414 alone owns the
   CMS capability projection.
5. Any source correction after a write/check invalidates the complete transaction and
   restarts the same recorded sequence from the corrected final source bytes. L02 and
   L03 may neither repair nor regenerate this corpus.

## Implementation Pseudocode

```ts
for (const vector of SECURITY_MATRIX) await assertRouteContract(vector);
await assertOneApplyForConcurrentSameScope();
await assertRollbackServerVerifiedSolutionKitRunCoverage({
  routes: ["generic", "curated"],
  headTransitions: ["C", "B", "A", null],
  statuses: ["success", "failed", "recovery_required"],
  rejectNonHead: true,
  preserveNonCuratedFallback: true,
});
await assertFencedHistoricalFormaDomReconciliation();
await assertCuratedRetentionBoundsAndDeleteTimeRecheck({
  perStarter: 512,
  total: 3_584,
});
await writeGuideSourcesAndRunExactTask548Transaction();
await assertGeneratedDocsAndSourceRelationsMatch();
```

Owned fixtures -> preview/apply/status/validate/rollback -> authoritative ledger and
owner projections -> shared server-verified rollback composite -> exact cleanup.
Historical FormaDom adoption runs only through the fixed reconciliation command under
the current writer fence. Retention rebuilds and rechecks the bounded root closure in
the delete transaction. Unknown errors are redacted. No truncation or raw payload
evidence; fixture cleanup uses source-run rollback and scoped deletes.

## Error Handling

Any matrix mismatch or cleanup failure fails the gate with a safe test identifier;
fixtures are still scoped and no broad delete/truncation recovery is allowed.
Any docs transaction/recovery/source-hash/coverage mismatch stops before L02. Never
paper over stale generated output or carry a pre-correction receipt forward.

## Testing Requirements

Cover four digest domains, artifact-before-DB failure, JSONB claim plus one-transaction
requested-run/lineage reservation CAS/replay/TTL, complete predecessor retention,
legacy nonterminal core/template/shell phases and compensation, provider-specific
setting transitions, committed-success full receipt recovery, deterministic audit UUID
conflict verification, Redis-one-outbox/memory-zero-outbox semantics, exhaustive error
map, route order, scoped Admin/Setup no-bypass with unchanged Assistant service path,
capability contribution, Setup post-await Finish snapshot, drift conflict, and
TASK-489 rollback relation resume/delegation.

- Through both the generic TASK-489 route and curated aliases, exercise
  `rollbackServerVerifiedSolutionKitRun` over `A -> B -> C` and prove exact head
  transitions `C -> B -> A -> null`, older non-head rejection before engine work, and
  exactly one engine-only fallback for a source with no curated evidence.
- Preserve exact `success|failed|recovery_required` results. For `failed`, prove zero
  TASK-555 second dispatcher call, settings restoration, rollback audit, or duplicate
  resource/history invalidation; preserve terminal counters and the curated head, but
  atomically clear every pending reservation field so a later explicit request is
  retryable. For `recovery_required`, preserve exact `summary:null`, HTTP 202, the
  unchanged head, and the fenced reservation so only authoritative resume may proceed.
  TASK-489's already-committed actual-resource/history invalidation remains present and
  is not suppressed or duplicated in either branch.
- Exercise the fixed historical FormaDom reconciliation command under the current
  writer fence: exact candidate adoption and idempotent replay pass, while ambiguous,
  changed, pending, mismatched, or raced evidence fails with zero lineage/run/item
  mutation.
- Pin `512` transitive links per starter and `3,584` distinct curated-chain runs total,
  including every retained item. Missing links, cycles, depth 513, or aggregate
  overflow delete nothing. Before each delete batch, lock/reload all seven lineage rows
  and candidate runs, rebuild the same bounded closure, and reject stale head,
  reservation, version, or candidate state before any child/parent delete.

```bash
set -a && source /home/coder/project/Coderso/.env && set +a && bun test --parallel=1 --timeout 360000 tests/integration/kits/curatedStarterAcceptance.test.ts tests/security/curatedStarter.security.test.ts tests/reliability/curatedStarter.reliability.test.ts tests/unit/release/codersoReleaseGates.test.ts
# Run the exact terminal TASK-548 recovery/write/check argv captured verbatim in
# the L01 start receipt. No convenience alias or reordered substitute is allowed.
bun run test
bun run precommit:check
bun run gates:coderso
bun run scan:security:strict
bun --cwd core lint:types
bun --cwd core lint
git diff --check
# Run wc -l over every added/modified human-authored production module and test file.
```

All touched human-authored production modules and test files must be <=1000 physical
lines. Documentation files and tool-owned generated outputs are outside that repository
gate; generated outputs are validated by byte/hash contracts instead.

## Documentation Updates Required

The complete product/security/cache/developer/Guide source update, all three Guide
relation files, twelve exact atoms, two ordered workflows, two exact localized section
bindings, six descriptor-parity IDs, and all terminal TASK-548-generated documentation
bytes are this leaf's deliverable. Hand the exact source/output inventory, transaction
argv and hashes, test/gate results, and any truthful skipped-gate note to L02. No
documentation or generated byte may change after this receipt without restarting L01
validation.
