# TASK-556-04-L02: Five-Flow Smoke Docs and Closure
# FileName: TASK-556-04-L02-Five-Flow-Smoke-Docs-And-Closure.md

**Parent Task:** TASK-556
**Parent Subtask:** TASK-556-04
**Priority:** High
**Category:** Runtime Smoke / Documentation / Closure
**Estimated Effort:** Very Large
**Dependencies:** TASK-556 external terminal gate; TASK-556-04-L01
**Start Receipt:** TASK-556-04-L01 reviewed landed diff; capability boundary tests green; all product receipts green
**Completion Receipt:** All docs/generated writes, gates, audits, and final L01 corpus read-checks completed before smoke; fast proved operational with no checkpoint; one final certification checkpoint passed owner-reviewed tracked evidence and unchanged-revision resume; only TASK-545 metadata closure followed
**Status:** ⏳ To Do
**Changelog:** 1270 pinned

---

## Overview

Register exactly five real flows through the shared runtime-smoke platform,
finish every non-metadata doc/release-gate byte and all validation before final
certification, produce one reviewed certification evidence set, then close only
TASK-545 metadata. Fast is operational non-checkpoint evidence and has no closure
authority. This leaf owns no product source.

## Sub-Tasks

None; this is an executable leaf.

## Exact Writer and Forbidden Paths

Sole writer paths:

- `scripts/runtime-smoke/adapters/task-556.ts`;
- cohesive modules under `scripts/runtime-smoke/adapters/task-556/` named
  `contracts.ts`, `fixture.ts`, `worker-operations.ts`, `database-batches.ts`,
  `browser-segments.ts`, `assertions.ts`, `cleanup.ts`, and `report.ts`;
- exact additive `task-556` registration regions in
  `scripts/runtime-smoke/contracts.ts`, `scripts/runtime-smoke/cli.ts`, and
  `scripts/runtime-smoke/registry.ts`;
- `tests/unit/runtime-smoke/task556-adapter.test.ts` and exact additive
  `task-556` cases in `tests/unit/runtime-smoke/cli-registry.test.ts`;
- exact additive TASK-556 command objects in
  `scripts/coderso-release-gates.ts` and focused source/config guard
  `tests/unit/release/task556ReleaseGateConfig.test.ts`, which also reads the
  committed workflows to pin the closure identity without editing them;
- `tests/README.md` and `_docs/CODERSO_RELEASE_GATES.md`;
- canonical final-certification evidence only under
  `_docs/_workflows/_smoke/evidence/task-556/task-556-certification/`;
- `_docs/ARCHITECTURE.md`, `_docs/CMS_SPEC.md`, `_docs/SOLUTION_KITS.md`,
  `_docs/ASSISTANT_SITE_BUILDER.md`, `_docs/CMS_API.md`, `_docs/DATA_MODEL.md`,
  `_docs/ORM_SPEC.md`, `_docs/RBAC_SPEC.md`, `_docs/PREVIEW_SPEC.md`,
  `_docs/SECURITY_SPEC.md`, `_docs/AUDIT_SPEC.md`, `_docs/TESTING_STRATEGY.md`,
  relevant `docs/develop/**` and `_docs/ADMIN_CACHE*.md` only when their shipped
  contracts changed; all `docs/guide/**` and tool-generated TASK-548/CMS outputs
  remain read-only from L01;
- one `_docs/_CHANGELOG/1270-<actual-closure-date>-task-556-formadom-code-owned-static-starter-designer-handoff.md`, its one README index row;
- only TASK-556 rows/statistics in `_docs/_TASKS/README.md` and all 13 TASK-556 files.

Forbidden paths: every product source/route/client/component/schema/migration and
  capability-owner source; every `docs/guide/**` or generated capability/docs
  byte; external task/changelog families and 1266-1269;
all three committed TASK-556 workflow entries, root config, `AGENTS.md`, `_TMP*`,
unrelated paths. Product failure returns to its
sole writer leaf; 04-L02 never patches product code.

Immediately before its first non-corpus docs/release-gate edit, L02 read-checks
the exact final L01 source and generated bytes with the landed terminal TASK-548
checks plus `cms-capabilities:check`; it runs no write command. Any needed Guide,
capability-source, or generated-byte correction returns to L01, invalidates the
L01 receipt and every downstream input, and requires L01's final-source write/
check sequence again before L02 resumes.

## Pinned TASK-545 Closure Identity

Before phase 1 is reachable, the tracked HEAD-identical
`_docs/_workflows/task-556-implement.mjs` must expose and pass static tests for
these exact literals:

```ts
const PINNED_TASK_ID = "TASK-556";
const PINNED_WORKFLOW_ROLE = "implement";
const PINNED_CHANGELOG_NUMBER = 1270;
const PINNED_CHANGELOG_SLUG =
  "task-556-formadom-code-owned-static-starter-designer-handoff";
const FINAL_CERTIFICATION_SESSION = "task-556-certification";
```

Only that `implement` entry may call TASK-545 phase 1 with those values and its
own `import.meta.url`. `task-556-author-audit.mjs`, `task-556-fix.mjs`, caller-
supplied workflow/path/role/number/slug values, and dynamic changelog allocation
have zero checkpoint or closure authority. The pinned changelog resolves only to
`_docs/_CHANGELOG/1270-<actual-closure-date>-task-556-formadom-code-owned-static-starter-designer-handoff.md`
through TASK-545's checkpoint-bound closure identity.

## Exact Additive Release-Gate Commands

Append the following command objects to their existing gate arrays without
deleting, replacing, weakening, or reordering any pre-TASK-556 command:

| Gate | Exact title | Exact added argv | DB flag |
|---|---|---|---|
| `functional` | `TASK-556 static starter functional flow` | `bun test --parallel=1 --timeout 360000 tests/integration/server/task556StaticGenerationRepository.test.ts tests/integration/server/task556StaticStageMaterialization.test.ts tests/integration/server/task556StaticStarterWorkspaceService.test.ts` | `requiresDatabaseUrl: true` |
| `ux` | `TASK-556 static starter UX` | `bun run test:vitest -- tests/vitest/ui-integration/customize-static-starter-in-designer.test.tsx tests/vitest/ui-integration/solution-kits-static-designer-slot.test.tsx tests/vitest/ui-integration/setup-static-designer-slot.test.tsx tests/vitest/admin/static-designer-navigation-accessibility.test.tsx` | absent |
| `performance` | `TASK-556 static starter persistence performance` | `bun test --parallel=1 --timeout 360000 tests/perf/designerStaticStarterPersistence.test.ts` | `requiresDatabaseUrl: true` |
| `performance` | `TASK-556 static starter stage performance` | `bun test --parallel=1 --timeout 360000 tests/perf/designerStaticStarterStage.test.ts` | `requiresDatabaseUrl: true` |
| `security` | `TASK-556 static capability security` | `bun test tests/security/designerStaticCapabilityBoundary.security.test.ts` | absent |
| `security` | `TASK-556 static persistence and route security` | `bun test --parallel=1 --timeout 360000 tests/security/designerStaticStage.security.test.ts tests/security/designerStaticStarterRoute.security.test.ts` | `requiresDatabaseUrl: true` |
| `reliability` | `TASK-556 static starter reliability` | `bun test --parallel=1 --timeout 360000 tests/integration/server/task556StaticGenerationMigration.test.ts tests/integration/server/task556StaticStageRecovery.test.ts` | `requiresDatabaseUrl: true` |

The gate runner already executes command objects sequentially. The two focused
performance files remain separate, use `--parallel=1`, and run with no smoke,
worker, or other load generator competing. The source/config test pins exact
titles/argv/order/DB flags, one additive registration per row above, and the
byte/order-preserved preexisting command prefixes. It also fails if a TASK-556
test is placed in the wrong runner lane, if the pure capability security test is
skipped with the DB-gated command, if a command is hidden behind a replacement,
or if `task-556-implement.mjs` lacks the exact task/role/number/slug while either
author-audit/fix entry exposes checkpoint authority.
This release-gate/config/docs work is non-product-source closure ownership.

## Exact Five Real Flows

Both `fast` and `certification` execute exactly these ordered IDs:

1. `provider-offline-direct-install-available`
2. `static-designer-seed-zero-agent-provider`
3. `setup-static-designer-seed-reopen`
4. `static-source-digest-rbac-idempotency-fail-closed`
5. `staged-preview-promotion-front-parity`

Scenario contracts:

1. Disable provider adapters, open real Solution Kits FormaDom detail, execute
   TASK-555 preview, confirm/apply, assert installed public visible effect and
   install-run receipt, execute TASK-555 rollback, and assert prior visible state
   restored. Button presence alone is invalid.
2. Click Solution Kits `Customize in Designer`, assert one private ready revision,
   visible Designer preview, one run-bound receipt, and zero provider/Agent/
   direct-installer/canonical-write evidence through bounded transport and DB
   canaries. Exact accessor/compiler/package-pass call counts remain injected
   integration/performance-test assertions, not black-box smoke telemetry.
3. In Setup with dirty name/locale, seed then navigate back/reopen; assert same
   workspace/revision, preserved dirty/step/Finish state, canonical href/focus,
   provider-offline usability, and unchanged run/receipt/stage identities on
   retained reopen/replay. Reload/manual deep link/auth-epoch change must return to Setup.
4. Exercise stale descriptor, malformed/oversized wire `Content-Length` before
   session resolution, missing session/each require-all permission,
   `admin_write` rate, CSRF, owner admission, content-type/invalid-JSON/strict-
   schema failures in exact terminal order, same-key changed request, fresh-key
   live collision/expired takeover, race-to-cap then expiry recovery, and cross-
   owner cases. Pin `invalid_json`, the terminal transport-owned cap code (currently
   `payload_too_large`), and separate `designer_static_seed_invalid`; assert safe
   statuses, no loser aliases, no duplicate roots/stage/canonical rows, and exact
   replay result with no new run/receipt/stage rows. Return to each real
   host and assert its bounded visible error/disabled state, focus target, and
   unchanged host state; API/DB rows alone are not visible evidence.
   The fixture creates an already expired DB-clock lease through its owned setup
   transaction; the smoke never waits 120 seconds or exposes a production clock/
   expiry override.
5. Seed a fresh fixture, preview at least three distinct routes and one interactive
   form/navigation effect while canonical Admin/public reads remain absent; approve
   and promote through terminal Designer, then assert front geometry/text/routes/
   interaction match approved preview and canonical visibility appears only after commit.

## Fixtures, Evidence, and Budgets

- One synthetic owner/session/role set per scenario; separate baseline namespace;
  owned-ID cleanup only, no truncation/global delete.
- Persistent bounded DB worker and shared supervised dev host; no fixed sleeps,
  one-process-per-query cleanup, or custom Playwright lifecycle.
- Bounded observation fixtures record only safe transport-attempt classes and
  before/after row-count/digest canaries through terminal TASK-414's shared
  `DesignerSmokeObservationV1`. They do not require production debug endpoints
  or claim private function-call telemetry. No production counter, telemetry
  field, debug route, or browser-visible marker is added for accessor/compiler
  call counts.
- Every scenario asserts visible computed style/geometry/DOM/route state, not control presence.
- Certification covers light/dark and 1440x900/390x844. Fast preserves one
  operational variant per flow and uses this exact ordered matrix without a
  TASK-545 manifest/checkpoint: flow 1 `light/1440x900`, flow 2
  `dark/1440x900`, flow 3 `light/390x844`, flow 4 `dark/390x844`, and flow 5
  `light/1440x900`. Adapter tests pin the complete profile/flow/theme/viewport
  assignment and fail theme collapse before evidence generation.
- Exactly one distinct PNG per scenario in fast and at least two per scenario in
  certification, named `<order>-<scenario-id>-<variant>.png`; no secrets/raw IDs.
- Zero console errors, page errors, failed requests except intentional negative
  responses; readiness timeout 30 s, scenario timeout 90 s fast/180 s certification,
  cleanup timeout 30 s; 100% fixture/session/process/port restoration.
- Reports contain exactly five scenario results plus infrastructure/cleanup fields,
  strict reject-unknown schema, phase timing and safe observation counts.
- `task-556-fast` uses only the shared runner's ignored or task-temporary
  operational output. It may retain one bounded redacted report for that
  invocation, but it creates no TASK-545 manifest/checkpoint, is never copied to
  or tracked under the certification inventory, is never staged, and has no
  closure authority.
- Complete all adapter/source/test/release-gate/non-metadata documentation writes,
  all L01 generated writes/checks, migration/Admin/security/full gates, line
  counts, diff checks, and post-audits before the final certification. Run fast
  only as operational feedback; any ensuing fix reruns affected gates/audits and
  fast before certification.
- With those bytes and receipts frozen, run certification once as
  `task-556-certification`. The sole canonical closure directory is
  `_docs/_workflows/_smoke/evidence/task-556/task-556-certification/`; before
  phase 1 it contains only `manifest.json`, its referenced strict `report.json`,
  and referenced screenshots. No fast artifact, extra file, symlink, or
  unreferenced artifact is accepted.
- Only the tracked HEAD-identical `task-556-implement.mjs` with role `implement`,
  changelog 1270, and the pinned literal slug invokes terminal TASK-545 phase 1
  for that certification result. Phase 1 binds HEAD plus the exact dirty
  revision/report/manifest/file hashes and workflow entry, creates exactly one
  `resume-checkpoint.json`, returns `owner_action_required`, and pauses.
- The owner reviews visible correctness plus secret/PII safety and stages only
  that one certification directory. The exact returned owning-workflow resume
  requires tracked parity, unchanged checkpoint hash/run/workflow and unchanged
  frozen non-evidence revision. After checkpoint creation it enters TASK-545
  metadata closure only: no implementation/fix/audit/gate/smoke/docs/generated
  command or non-metadata edit may run. Terminal closure-delta permits only all
  13 TASK-556 files, their board rows/statistics, changelog 1270 at the exact
  pinned slug, and its index row; no bytes change after the final revision proof.
- `database-batches.ts` owns one scenario ledger: every created/updated canonical
  resource is registered through terminal TASK-414's shared
  `DesignerSmokeFixtureLedgerV1`; this adapter wraps rather than copies that
  restoration primitive. Cleanup requires exact scenario after-state, uses the
  shared generation-consistent CAS/native-adapter transaction, observes cache/
  search restoration, and then proves all private bindings/aliases/runs/stage/
  receipts and promoted fixtures absent. Any drift/foreign reference blocks
  cleanup and closure; no truncation, latest-row fallback, broad prefix delete,
  direct pointer rewrite, or task-local process loop is allowed.

## Implementation Pseudocode

```ts
const task556Adapter: SmokeAdapter = {
  suiteId: "task-556",
  supportedProfiles: ["fast", "certification"],
  async run(context) {
    return runTask556ScenariosWithSharedLifecycle(context);
  },
};

export default task556Adapter;

// Pre-authored tracked task-556-implement.mjs orchestration; the adapter does
// not import TASK-545 or create checkpoints.
await requireAllNonMetadataWritesGatesAndAuditsComplete();
await runSharedSuite({
  suite: "task-556",
  profile: "fast",
  session: "task-556-fast",
  checkpoint: false,
});
await requireNoFastCheckpointOrClosureInventory();
const finalResult = await runSharedSuite({
  suite: "task-556",
  profile: "certification",
  session: FINAL_CERTIFICATION_SESSION,
});
return createResumeCheckpoint({
  repoRoot,
  expectedTask: PINNED_TASK_ID,
  pinnedChangelogNumber: PINNED_CHANGELOG_NUMBER,
  pinnedChangelogSlug: PINNED_CHANGELOG_SLUG,
  expectedWorkflowRole: PINNED_WORKFLOW_ROLE,
  executingImportMetaUrl: import.meta.url,
  runtimeResult: finalResult,
}); // exactly one owner_action_required checkpoint; stop immediately
```

**Data flow:** all docs/generated writes + gates/audits -> operational fast shared
lifecycle/cleanup with no closure artifact -> unchanged final certification ->
owned fixtures/observations -> real browser/API/DB flows -> visible assertions/
screenshots -> strict report -> set-based cleanup/restoration -> one TASK-545
phase-1 checkpoint -> owner review/stage -> metadata-only resume.

**Errors:** harness failures retain scenario/checkpoint and safe observation names;
product failures return to owner leaf; cleanup failure keeps closure open; never
report cookies, CSRF, package bodies, private docs, SQL, raw responses or secrets.

## Tests and Closure

- Adapter has exactly five ordered IDs in both profiles; unknown/reordered/sixth
  scenario rejects; observations and cleanup schema strict.
- `tests/README.md` registers `task-556` in the shared runtime-smoke suite list,
  documents both exact profile commands and five IDs, and records Bun ownership
  of DB/performance/security/reliability versus Vitest ownership of pure/UI tests.
- `_docs/CODERSO_RELEASE_GATES.md` documents the additive five-lane command
  mapping, DB requirements, serial no-competing-load performance rule, and that
  existing commands remain mandatory.
- Aggregate guards retain the terminal TASK-414/TASK-555 guards recorded at the
  start gate unchanged: exact prefixless/pre-body route transport and error
  order, composite child/global purge indexes, actor/epoch-scoped Designer cache
  invalidation with no private payload, and the pinned light/dark profile matrix.
  Closure must not rebaseline or weaken those assertions.
- Finish all non-metadata documentation/release-gate/adapter writes, run all family
  targeted tests and full mandatory commands, and resolve post-audits before the
  final certification.
- Fresh post-audit lenses: scope/identity/persistence, security/API, UI/direct-install,
  capability/Agent boundary, test/smoke/closure integrity. Resolve every H/M/L.
- Run focused harness self-tests and fast from the restarted dev host, assert fast
  created no checkpoint/canonical closure inventory, then read-check the immutable
  L01 Guide/TASK-548/CMS outputs one final time. Never run a corpus/capability write
  command in L02.
- With no subsequent non-metadata mutation or gate, run final certification once.
  Only its manifest/report/screenshots enter the canonical inventory.
- Run TASK-545 phase 1 only for certification through the exact tracked HEAD-
  identical `task-556-implement.mjs` and pinned role/number/slug. It creates one
  checkpoint and pauses. After owner review/staging, the emitted resume proves the
  frozen revision and performs only metadata closure/final closure-delta; it does
  not rerun any gate, audit, smoke, docs check/write, or implementation phase.
- Changelog 1270 title/body explicitly enumerates all 13 IDs:
  `TASK-556`, `TASK-556-01`, `TASK-556-01-L01`, `TASK-556-01-L02`,
  `TASK-556-02`, `TASK-556-02-L01`, `TASK-556-02-L02`, `TASK-556-03`,
  `TASK-556-03-L01`, `TASK-556-03-L02`, `TASK-556-04`,
  `TASK-556-04-L01`, `TASK-556-04-L02`.
- Its path uses the actual checkpoint-owned UTC closure date and exact literal
  slug `task-556-formadom-code-owned-static-starter-designer-handoff`; no dynamic
  scan/allocation or alternate slug is accepted.
- Terminalize leaves before children before parent; update only TASK-556 board
  rows/statistics after all evidence is green.

## Security Contract

- **Visibility:** no new endpoint; real internal seed/direct install/preview/
  promotion and promoted public reads only.
- **Authentication/RBAC:** synthetic sessions and exact production permissions;
  no test bypass.
- **CSRF/rate:** production CSRF/rate paths, including negative assertions.
- **Validation:** strict fixture/observation/scenario/report/cleanup/evidence schemas.
- **Anti-abuse:** no new public write; promoted forms retain native protection.
- **Privacy:** redact all session/token/raw/private/package/SQL data from evidence.

## Testing Requirements

```bash
set -a && source .env && set +a
bun --cwd core lint:types
bun --cwd core lint
bun run lint:repo:types
bun run docs:check
bun run cms-capabilities:check
bun test tests/unit/runtime-smoke/task556-adapter.test.ts tests/unit/runtime-smoke/cli-registry.test.ts
bun test tests/unit/release/task556ReleaseGateConfig.test.ts
bun test --parallel=1 --timeout 360000 tests/perf/designerStaticStarterPersistence.test.ts
bun test --parallel=1 --timeout 360000 tests/perf/designerStaticStarterStage.test.ts
bun run test
bun run precommit:check
bun run gates:coderso
bun run scan:security:strict
bun --cwd core build:admin
bun run check:admin-boundary
bun run check:admin-bundle
node --check _docs/_workflows/task-556-author-audit.mjs
node --check _docs/_workflows/task-556-implement.mjs
node --check _docs/_workflows/task-556-fix.mjs
git diff --check
# Finish migration parity/upgrade, targeted owner suites, line counts, and all
# post-audits here. No docs/generated/source/test/workflow/gate write follows.
bun scripts/runtime-smoke.ts run --suite task-556 --profile fast --session task-556-fast
# Assert fast produced no TASK-545 checkpoint, canonical closure file, or staged path.
bun scripts/runtime-smoke.ts run --suite task-556 --profile certification --session task-556-certification
# task-556-implement.mjs alone invokes TASK-545 phase 1 with role implement,
# changelog 1270, and the pinned literal slug; stop on owner_action_required.
# After owner review/staging, invoke only the emitted phase-2 resume for metadata closure.
```

Run the focused performance command without competing tests/smokes/workers. Run
migration parity/upgrade and every targeted owner suite. Run `wc -l` across
every added/modified human-authored production/test file from the family baseline;
fail above 1,000. Those checks and all other gates finish before certification.
State any unavailable lane and do not close. After phase 1 creates the checkpoint,
only TASK-545's tracked-evidence validation, ordered durable changelog/index write,
TASK-556 status/board metadata writes, and final metadata-delta validation may run.

## Documentation Updates Required

Update `_docs/ARCHITECTURE.md`, Solution Kits/Setup/Designer/API/data/preview/
security/audit docs and relevant non-corpus developer/cache/smoke docs,
`tests/README.md`, and `_docs/CODERSO_RELEASE_GATES.md` before final
certification. After its checkpoint, update only changelog 1270/index, TASK-556
board/statistics, and all family statuses through TASK-545 metadata closure with
exact validated receipts.
Read-check but do not edit `docs/guide/**` or generated TASK-548/CMS bytes. Do not
edit external task families or foreign changelogs.
