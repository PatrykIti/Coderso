# TASK-556-04: Capability Reconciliation Acceptance and Closure
# FileName: TASK-556-04-Capability-Reconciliation-Acceptance-And-Closure.md

**Parent Task:** TASK-556
**Priority:** High
**Category:** Capability / Runtime Smoke / Documentation / Closure
**Estimated Effort:** Very Large
**Dependencies:** External gate from TASK-556; TASK-556-03 landed receipt green
**Status:** ⏳ To Do
**Changelog:** 1270 pinned

---

## Overview

Join the landed static route/control/workflow evidence and terminal Designer
facts into `CmsCapabilityManifestV1` through the existing compiler, run
exactly five shared real flows, then close docs/changelog/board/status. This
child creates no second capability schema/compiler/generator and cannot repair unrelated
product source.

## Sub-Tasks

| Order | ID | Scope | Status |
|---:|---|---|---|
| 1 | TASK-556-04-L01 | Capability boundary and negative authorization tests | ⏳ To Do |
| 2 | TASK-556-04-L02 | Five real smokes, docs, changelog 1270, board/status closure | ⏳ To Do |

L01 consumes TASK-556-03's complete receipt; L02 consumes L01's reviewed receipt.

## Capability Contract

Use terminal `CmsCapabilityManifestV1` without a TASK-556 capability union.
Local provider-free seed/reopen is represented by one additive active feature row
with stable ID `core:designer/code-owned-static-starter` and exact Admin route/
control/Guide workflow evidence. L01 adds its exact file/export pair to terminal
`cmsCapabilitySourceInventory.ts`. Workflow reciprocity permits one additional
workflow ID only in `guide.composedWorkflows` for the pinned terminal Designer
feature rows `core:designer/workspace-preview` and
`core:designer/workspace-promotion`; every other field and every other pre-TASK-556
row remains byte-identical. Different terminal feature IDs fail the start gate and
require contract correction before writes.
`designer.stage`, `designer.preview`, `designer.validate`, and
`designer.promote` reuse the exact terminal supported adapter/permission/bounds
facts; Agent `inspect`, `research`, `plan`, and `mutate` are each unavailable
with the existing closed reason `product_not_applicable`. These facts never
authorize. Later AI revision remains under terminal Designer provider policy.
L01 contributes through the landed TASK-414 capability source registry, runs
all terminal TASK-548 and CMS capability write/check commands only after its
final Guide/capability source bytes, and proves every pre-TASK-556 manifest row
byte-identical except the one additive workflow ID in
`guide.composedWorkflows` on the exact pinned preview/promotion rows. L02 treats
all corpus/generated bytes as read-only; a needed
Guide correction returns to L01 and invalidates its receipt.

## Exact Ordered Smoke IDs

1. `provider-offline-direct-install-available`
2. `static-designer-seed-zero-agent-provider`
3. `setup-static-designer-seed-reopen`
4. `static-source-digest-rbac-idempotency-fail-closed`
5. `staged-preview-promotion-front-parity`

## Security Contract

- **Visibility:** no new route; tests use existing internal and promoted public flows.
- **Auth/RBAC/CSRF/rate:** assert exact production policies, never test-only bypasses.
- **Validation:** strict generated/smoke/report/cleanup objects and bounded evidence.
- **Anti-abuse/privacy:** synthetic owned fixtures; no secrets/raw payloads in artifacts.

## Testing Requirements

Run both leaf commands, focused persistence/stage performance files, the TASK-556
release-gate source/config test, all five additive release-gate lanes,
migration/Admin/security gates, post-audits, and the operational fast smoke before
the final certification. Only certification enters TASK-545's two-phase owner-
reviewed tracked-evidence and unchanged-revision resume through workflow role
`implement`, changelog 1270, and the pinned literal slug. After checkpoint, run
only TASK-545 metadata closure/delta checks. Report unavailable lanes truthfully.

## Documentation Updates Required

L01 owns capability-boundary source/tests, every Guide/capability source edit,
and all terminal TASK-548/CMS generated bytes. L02 only read-checks that frozen
corpus/output and owns smoke, `tests/README.md`,
`_docs/CODERSO_RELEASE_GATES.md`, release-gate registration, non-corpus docs,
changelog 1270/index, board/statistics, and all 13 status updates. Its non-metadata
writes finish before certification; changelog/index/board/status are the only
post-checkpoint writes and run through TASK-545 metadata closure.
