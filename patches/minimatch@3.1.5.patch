diff --git a/minimatch.js b/minimatch.js
index 2e4a058a130dceebf83a7bd435f5d22b765ac836..2164bf3384cd5a8015ab58345bbcceb7c6d7616f 100644
--- a/minimatch.js
+++ b/minimatch.js
@@ -9,4 +9,8 @@ minimatch.sep = path.sep
 var GLOBSTAR = minimatch.GLOBSTAR = Minimatch.GLOBSTAR = {}
-var expand = require('brace-expansion')
-
+// brace-expansion 5.x exports `expand` as a named export instead of
+// `module.exports = expand`. Accept both shapes so this compatibility patch
+// stays correct if the root security override is ever relaxed across majors.
+var braceExpansion = require('brace-expansion')
+var expand = typeof braceExpansion === 'function' ? braceExpansion : braceExpansion.expand
+
 var plTypes = {
