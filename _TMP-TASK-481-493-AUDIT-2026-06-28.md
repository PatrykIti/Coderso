All key high/medium findings verified against source. Compiling the synthesized report.

---

# READ-ONLY Drift + AGENTS.md-Compliance Audit — TASK-481..493

Audit scope: the 13 task trees authored in commit `a85414c4` (`docs(tasks): break down TASK-481 + author 12 feature sibling tasks (482-493)`). 196 task files under `_docs/_TASKS/`. STRICT READ-ONLY; no files edited, no state-changing git run. TASK-478/479/480 were not touched (one 479 file read solely to verify the declared 482↔479-29 handoff).

## 1. Executive summary

The 13 trees are, with two structural exceptions, **AGENTS.md-compliant, well-formed, and broadly execution-ready**. The known defect classes are clean across all 196 files: filenames == `# FileName:`, H1 id == physical id, umbrella slugs underscore / child slugs hyphen, every child carries a correct `**Parent Task:**` / `**Parent Subtask:**`, `**Status:**` is exactly `⏳ To Do` everywhere (no inline dates, no stray enum values), NN/LNN numbering is contiguous, every umbrella Sub-Tasks table and subtask leaf table matches disk, and all 13 umbrella rows exist in the README To Do board with correct counts.

The trees are **not drift-free**. The headline risks:

- **One confirmed High (cross-tree drift): TASK-482 plans to reuse `AuthBrandPanel.tsx`, which its own dependency TASK-479-29 deletes.** 482's header dep and reuse table assert 479-29 "Owns `AuthBrandPanel.tsx` restyle" and 482 "Reuses it"; 479-29-L01:110-112 says the brand panel is "no longer used … leave it unreferenced (or delete it)" and a centered `AuthShell` replaces it. An implementer following 482 would build the installer on a removed component.
- **Migration-index collision: three trees (483, 484, 493) all hard-code the next index `0064`.** Disk head is `0063_yummy_glorian` (journal idx 63, version 7), so 0064 is correct for whichever lands first and wrong for the other two. No coordination note exists.
- **Shared boundary `core/server/publicSite.tsx` is mutated by four trees (483/486/491/493) with no reciprocal references and competing nonce strategies.**
- A cluster of **pseudocode-vs-core drift** (fabricated imports/symbols, wrong signatures, stale line anchors) that would fail typecheck if copied literally — concentrated in 482/483/486/493.
- Two structural non-compliances: **TASK-481 umbrella is missing `Started`/`Completed`** (uses non-canonical `Created`), and **TASK-493-03 has an invalid `Priority: Large`** (Large is an Effort value).

None of these are blocking at the tree level — all are fixable inside 481-493 files. The two trees marked "not execution-ready" (483, 486, 492) carry medium pseudocode/test-lane defects that need a fix pass before implementation.

## 2. Verdict by severity (after verification & dedup)

| Severity | Count | Notes |
|---|---|---|
| Critical | 0 | none found |
| High | 1 | 482 AuthBrandPanel reuse-vs-delete drift (confirmed) |
| Medium | 13 | incl. migration-0064 collision (1 finding, 3 trees) and publicSite overlap (1 finding, 4 trees) after dedup |
| Low | ~24 | stale line anchors, illustrative-helper drift, test-lane/wording nits |
| Info | ~9 | placeholder-style divergence, board statistic, baseline-verified notes |

One finding was downgraded by adversarial verification: **486-01-L03 cookie-parsing was raised High, verdict = confirmed, correctedSeverity = Medium** (applied below). No findings were refuted.

## 3. Confirmed Critical & High findings

| sev | category | tree | file | evidence | recommendation |
|---|---|---|---|---|---|
| **High** | drift-stale-assumption | 482 | `_docs/_TASKS/TASK-482_Setup_And_Onboarding_Wizard.md` (line 7 dep; line 77 reuse table) — also `TASK-482-03-L01-Installer-Wizard-UI.md` | 482 claims 479-29 "Owns `AuthBrandPanel.tsx` restyle" and 482 "**Reuses** it for the installer". Verified `TASK-479-29-L01`:110-112 (read for handoff only): "the gradient `AuthBrandPanel` split column is **no longer used** … leave `AuthBrandPanel.tsx` in place but unreferenced (or delete it … grep first); the centered layout replaces it." 479-29-L01:84 makes the centered `AuthShell` the new default. | In the 482 files, reword the handoff to reuse the **new centered `AuthShell` + restyled auth primitives**, not `AuthBrandPanel`. Drop `AuthBrandPanel` from 482-03-L01's "reuse" list. Fix lives entirely in 482 files. |

No Critical findings.

## 4. AGENTS.md compliance

**Naming / headers / parent-fields / status / leaf-counts / numbering / board-sync** — all PASS across 196 files, with two exceptions:

- **TASK-481 umbrella missing canonical date fields (Medium, headers).** `_docs/_TASKS/TASK-481_Page_Editor_Canvas_Brand_Token_WYSIWYG.md` header (lines 5-10) is Priority/Category/Effort/Dependencies/Status/`**Created:** 2026-06-27` — no `**Started:**`/`**Completed:**`. Every other umbrella (482-493) and all of 481's own children carry both placeholders. `Created` is not in the canonical date-field set. This is the sole reason 481 is non-compliant.
- **TASK-493-03 invalid Priority enum (Medium, headers).** `_docs/_TASKS/TASK-493-03-Google-Search-Console-Integration.md`:5 reads `**Priority:** Large`. "Large" is an Estimated-Effort value (same file line 7 is `**Estimated Effort:** Large`); both child leaves are `**Priority:** High`. Copy-paste from the Effort line.

**Security-contract coverage** — every executable leaf carries a `## Security Contract` except legitimately-exempt test-only leaves. Noted exempt/consistency nits: `TASK-485-01-L03` (pure Vitest client/cache test — exempt) omits it where its sibling test leaves include one.

**Pseudocode coverage** — every executable leaf has `## Implementation Pseudocode` except **`TASK-489-03-L02-Docs-And-Closure.md`** (verified: headings are Overview / Legacy-test reconciliation / Documentation Updates / Closure Gate Matrix / Testing Requirements — no pseudocode heading), while the analogous `TASK-490-02-L03-Docs-And-Closure.md` does include one (verified). Low/consistency; the "Legacy-test reconciliation" section already functions as the change recipe.

**Test-lane assignment** — mostly correct; confirmed defects: `TASK-490-01-L02` and `TASK-490` umbrella/subtask cite `tests/security/codersoSecurityGate.test.ts` for a route-permission/bucket assertion that file cannot host (Medium); `TASK-492-02-L02` and `TASK-492-03-L02` route/db-backed test plans assume harness capabilities the existing suites lack (Medium, see §3-adjacent mediums). One rationalized deviation: `TASK-484-01-L02` keeps a pure `computeNextRunAt` in the Bun unit lane for cohesion with the existing `bun:test` backup suite (Info, accepted).

**Migration-artifact coverage** — all migration leaves declare `.sql` + `meta/NNNN_snapshot.json` + journal-idx artifacts. The single defect is the **shared hard-coded index 0064** (see §7).

## 5. Drift vs code (fabricated / renamed / stale references)

Verified against core:

| tree | file | drift | reality |
|---|---|---|---|
| 482 | `TASK-482-01-L01-FirstRun-Service.md` | imports `DbTransaction` from `core/db/client` | `core/db/client.ts` exports **only** `db` (`:13`); `DbTransaction` is a private inline alias in `settingsService.ts:11` (`Parameters<Parameters<typeof db.transaction>[0]>[0]`). Replicate the alias locally. (Medium) |
| 482 | `TASK-482-02-L02-Install-Admin-Route.md` | "Add it to the CSRF **exemption list** the same way `/auth/login`/`/auth/reset` are exempt" | `core/server/middleware/csrf.ts` has **no path list**; it auto-skips when `ctx.sessionId` is absent. The session-less install POST inherits the skip; no CSRF code change needed. (Medium) |
| 482 | `TASK-482-03-L01-Installer-Wizard-UI.md` | wraps form in `<AuthShellLayout …>` | no `AuthShellLayout` exists under `core/admin/ui/`; the leaf's own reuse list cites the real `AuthBrandPanel`. Compose inline as `LoginPage.tsx` does, or label as new. (Info) |
| 483 | `TASK-483-02-L02-Public-Ingestion-Route-And-Bun-Serve-Wiring.md` | `checkRateLimit("public_write",{ip,userAgentHash:hashUa(req)}); if(!limit.ok) throw …429` | verified `core/server/middleware/rateLimit.ts:48`: `checkRateLimit(bucket, identity:{ip,userAgent?,identifier?}, config: SecuritySettings["rateLimit"], options?)`; returns **void** and **throws** `ApiError(429)` itself — no `.ok`, no `userAgentHash`/`hashUa`. Real callers (`publicBookingApi.ts:321`) pass the 3-arg form. Literal copy fails typecheck (missing `config`). (Medium) |
| 486 | `TASK-486-01-L03-Public-Popups-Route-And-Wiring.md` | builds `routeCtx={headers:{},cookies:{}}`, copies `req.headers`, calls `attachUserFromSession`, sets `isLoggedIn=Boolean(routeCtx.user)` | verified `core/server/middleware/auth.ts:19`: `attachUserFromSession` reads `ctx.cookies?.[cookieName]`; the cookie header is never parsed into `routeCtx.cookies`, so `user` is always undefined → `isLoggedIn` always false, silently breaking session-derived audience. Parse cookies first (mirror booking/forms). (High→**Medium** by verification) |
| 486 | `TASK-486-02-L03-Runtime-Orchestrator.md` | imports `matchPopupTargeting` but never uses it; advertises a client path/audience guard | `TASK-486-01-L01` DTO deliberately strips targeting, so a client guard is impossible; the unused import is a lint failure. Drop import; reword guard as "SPA nav re-fetches `/api/popups`". (Medium) |
| 486 | `TASK-486-03-L02-Runtime-Script-Injection.md` | `SERIALIZED=[scrollDepthPercent,…]` but import omits `scrollDepthPercent` | add it to the `triggerWatchers` import or typecheck fails. (Low) |
| 481 | `TASK-481-02-Brand-Token-Canvas-Emission-And-Live-Wiring.md` | `toCssVariableMap:703` | `toCssVariableMap` is at `core/ui/theme/tokenCss.ts:113`; cited lines 115-121 are correct. `:703` is a copy-pasted pageRendererV2 line. (Low) |
| 481 | `TASK-481-03-L01-Live-Palette-Inline-Text-Color-Toolbar.md` | invents `data-page-editor-text-mark-color` | real attribute is `data-page-editor-text-color-swatch` (`PageAuthoringCanvas.tsx:471`). (Low) |
| 481 | `TASK-481-03-L02-Inline-Block-Canvas-Preview-Agreement-Test.md` | reuses helper `makeTokens()` "already in the test file" | `makeTokens` exists nowhere; build tokens via `mergeTokens(DEFAULT_TOKENS,…)`. (Low) |
| 493 | `TASK-493-01-L01-Schema-Tables-And-Domain-Types.md` | states `numeric` "already imported at `:1-13`, already used elsewhere" | verified `core/db/schema.ts:1-13` imports `pgTable,uuid,text,timestamp,jsonb,integer,boolean,primaryKey,uniqueIndex,index,AnyPgColumn` — **no `numeric`**; the only pre-existing "numeric" token is a comment (`:783`). `numeric` must be **added** to the import. (Medium — false precondition) |
| 493 | `TASK-493-05-L01-Seo-Manager-Real-Data-Rewire.md` | assumes 479-26-L02 renders an "Indexed pages" placeholder stat to flip | per the cross-tree handoff check, 479-26-L02 renders "Optimized pages", not "Indexed pages". Reconcile the label inside the 493 file. (Medium, handoff drift) |

Additional stale-but-locatable line anchors (all symbols exist, Low): `482-07-L02` `resolveSessionTtlDaysFromSources` (def `sessionService.ts:114`, cited 179-186 = call site); `482-01-L02`/`02-L02` auth-bucket lines (actual `httpServer.ts:328/332`); `487-03-L01` `normalizeTags` (def `:411`, cited `:893` = call site); `493-03-L01` `value:null` (`integrationsService.ts:101`, cited `:94`). Illustrative-helper drift (Low): `488-01-L02` `toIntOrZero` (use existing `parseIntegerOrNull` `commerceEditorModel.ts:27`); `487-01-L01` reinvents `areEntrySnapshotsEqual` (reuse `areRevisionSnapshotsEqual` from `revisionSnapshot.ts`); `490-01-L01` `formatCell` comment overstates parity (real `formatPayloadValue` returns `"-"`, builder emits `""`).

## 6. Per-tree status table

| tree | compliant | execution-ready | drift-free | #confirmed findings (H/M/L+) |
|---|---|---|---|---|
| 481 | **No** (umbrella headers) | Yes | No | 0H / 1M / 3L |
| 482 | Yes | Yes | No | **1H** / 2M / 4 info/L |
| 483 | Yes | **No** | No | 0H / 3M / 2L |
| 484 | Yes | Yes | Yes | 0H / 2M / 1L + 2 info |
| 485 | Yes | Yes | Yes | 0H / 0M / 2L + 2 info |
| 486 | Yes | **No** | No | 0H / 2M (incl. downgraded) / 1L + 1 info |
| 487 | Yes | Yes | Yes | 0H / 1M / 2L |
| 488 | Yes | Yes | Yes | 0H / 0M / 2L |
| 489 | Yes | Yes | Yes | 0H / 0M / 1L + 1 info |
| 490 | Yes | Yes | No | 0H / 1M / 2L |
| 491 | Yes | Yes | Yes | 0H / 0M / 3L + 1 info |
| 492 | Yes | **No** | Yes | 0H / 2M / 2L + 1 info |
| 493 | **No** (493-03 Priority) | Yes | No | 0H / 4M / 2L |

## 7. Cross-tree coherence

- **Migration index 0064 collision (Medium — 3 trees).** Verified disk head `0063_yummy_glorian` (journal idx 63, version 7), next free 0064. `TASK-483-01-L02`, `TASK-484-01-L01`, `TASK-493-01-L02` (and their subtasks) each hard-code `0064_<slug>.sql` + `meta/0064_snapshot.json` + journal idx 64. Only one can win. Each must say the index is allocated at implementation/merge time (regenerate via drizzle-kit), in dependency order; whichever merges later renumbers (0065/0066).
- **`core/server/publicSite.tsx` shared boundary (Medium — 4 trees).** Verified references in 483 (`02-L02`, `03-L02`), 486 (`01-L03`, `03-L01`, `03-L02`), 491 (`01-L01`, `01-L02`), 493 (`02-L01`). No mutual coordination notes; analytics-beacon and snippet-injection nonce strategies are described independently. Add reciprocal notes and converge on one nonce mechanism.
- **`mapAnalyticsError` double-ownership (Low, within 483).** `02-L02` and `04-L03` both claim to "own" the net-new symbol in `analyticsRoutes.ts`. Designate `02-L02` lands first; `04-L03` extends; enumerate the full error set.
- **479/480 handoff accuracy.** 480 (dashboard) not in scope and not referenced by these trees. **479 handoffs are the weak spot:** the 482↔479-29 reuse (High, §3) and 493-05-L01↔479-26-L02 label (Medium, §5) both rest on stale 479 assumptions. Note: 479 is owned by another agent and is being concurrently edited — **the fix must be made in the 482/493 files**, never by editing 479.
- **492↔TASK-359-07 (Low).** 492-03-L01 finishes persistence the live `LoginAlertsPage` disclaimer attributes to TASK-359-07 (`Status: Done`), without acknowledging the supersession or its divergent `channels`/`bruteForceThreshold` payload. Add a one-line "supersedes 359-07's unimplemented persist path" note in the 492 file.

## 8. Prioritized fix list

All fixes below touch **ONLY** `_docs/_TASKS/TASK-481*..TASK-493*` files. **Do not edit any TASK-478/479/480 file** even where a handoff is wrong — correct the wording on the 481-493 side.

1. **[High] 482 AuthBrandPanel.** In `TASK-482_Setup_And_Onboarding_Wizard.md` (line 7 dep + line 77 reuse table) and `TASK-482-03-L01-Installer-Wizard-UI.md`, change "Reuses `AuthBrandPanel.tsx`" to "Reuses the new centered `AuthShell` + restyled auth primitives from 479-29 (`AuthBrandPanel` is removed by 479-29)". Drop `AuthShellLayout` from the 03-L01 pseudocode or label it new.
2. **[Med] Migration 0064 collision.** Add an identical coordination note to `TASK-483-01-L02`, `TASK-484-01-L01`, `TASK-493-01-L02` (and the three subtask files): "Index is provisional; re-derive as last-shipped+1 via drizzle-kit generate at implementation time — if a sibling 483/484/493 migration lands first, bump (0065/0066)."
3. **[Med] 493-03 Priority enum.** `TASK-493-03-Google-Search-Console-Integration.md:5` → `**Priority:** High`.
4. **[Med] 481 umbrella headers.** Add `**Started:** \`<YYYY-MM-DD>\`` and `**Completed:** \`<YYYY-MM-DD>\`` to `TASK-481_Page_Editor_Canvas_Brand_Token_WYSIWYG.md`; drop or demote `**Created:**`.
5. **[Med] 493-01-L01 numeric.** Reword to: "`numeric` is **not** currently imported/used — add it to the `drizzle-orm/pg-core` import in `schema.ts`."
6. **[Med] 483-02-L02 checkRateLimit.** Rewrite Security Contract + pseudocode to `checkRateLimit("public_write", { ip, userAgent }, security.rateLimit)`; rely on its thrown 429 (catch + remap to `analytics_rate_limited`). Remove `.ok`/`userAgentHash`/`hashUa`.
7. **[Med] 482-01-L01 DbTransaction.** Replace the bogus import with a local alias `type DbTransaction = Parameters<Parameters<typeof db.transaction>[0]>[0]` (mirror `settingsService.ts:11`).
8. **[Med] 482-02-L02 CSRF.** Rewrite to: csrf.ts auto-skips on session-absence; the session-less install POST inherits it — no CSRF code change. Assert the absence-based skip.
9. **[Med] 486-01-L03 cookies.** Add cookie-header parsing into `routeCtx.cookies` before `attachUserFromSession` (mirror booking/forms).
10. **[Med] 486-02-L03.** Remove the unused `matchPopupTargeting` import; reword the client path/audience guard to "SPA nav re-fetches `/api/popups`".
11. **[Med] 483-02-L02 campaign attribution.** Either add a `url`/UTM query field to `TASK-483-01-L01` `RawTrafficEvent`/`trafficEventSchema` + client payload, or remove the campaign branch and mark it out-of-scope. Keep L01/02-L02/03-L01 aligned.
12. **[Med] 484-04-L01 restore transaction.** Thread the outer `tx` into a transaction-aware importer (`importConfigTx` reusing `setSettingsTx`) or soften the "single transaction" claim (`importConfig` opens its own tx today).
13. **[Med] 490-01-L02 test lane.** Drop the `codersoSecurityGate.test.ts` citation; assert route registration + `requirePermission("forms:read")` in `tests/integration/routes/forms.test.ts`.
14. **[Med] 492-02-L02 / 492-03-L02 test lanes.** Either widen `AuthRouteDeps` injection (and say so) for the login happy-path, or move the assertion to a service-level test; gate the new db-backed gate test with the repo `testIfDb` pattern or relocate the cleartext round-trip out of the static gate file.
15. **[Med] 493-05-L01 label.** Reword to "ADDS/relabels an 'Indexed pages' StatCard backed by `overview.indexedPages`" instead of "flips the existing Indexed-pages placeholder".
16. **[Med] publicSite.tsx overlap.** Add reciprocal coordination notes across 483/486/491/493 and converge on one nonce mechanism.
17. **[Low] batch.** Fix stale anchors and illustrative-helper drift listed in §5 (`481-02` :703, `481-03-L01` data-attr, `481-03-L02` makeTokens, `482-07-L02`, `487-03-L01`, `493-03-L01`, `488-01-L02` toIntOrZero, `487-01-L01` reuse `areRevisionSnapshotsEqual`, `486-03-L02` scrollDepthPercent import, `485-03-L01` regex, `485-03-L02` put/delete prose, `490-01-L01` formatCell comment, `491-04-L01` `isParseableSentryDsn` owner + deps line, `487-02-L01` "extend not add" entriesClient.test.ts). Optionally add a thin `## Implementation Pseudocode` (or explicit N/A) to `489-03-L02`.

Info-level (no action / owner-confirm): Started/Completed placeholder style varies across trees (cosmetic); README Statistics "To Do: 360" not independently reconcilable from per-file grep (board rows themselves correct) — owner to confirm the aggregate was incremented.

=== confirmed C/H: 1/2 ===